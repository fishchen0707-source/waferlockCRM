# WAFERLOCK CRM 版本紀錄（CHANGELOG）

> ⚠️ **協作守則：動手改任何東西之前，請先讀完本文件。**
> 改完之後，**務必在最上方「未發布」區補一筆**（日期、做了什麼、誰做的、影響範圍），再 commit。
> 這份文件是「目前做到哪、還缺什麼」的單一真實來源（single source of truth）。
> 詳細的系統架構請另見 [`專案總覽.md`](專案總覽.md)。

---

## 📍 目前狀態快照（2026-06-23）

- **主檔**：`waferlock_crm.html`（單一 HTML，React CDN + Babel，無建置工具）
- **後端**：Supabase（URL/anon key 硬編碼於前端；RLS 目前全開）
- **線上**：GitHub Pages，主分支 `main` 一 push 即自動部署
- **遠端**：https://github.com/fishchen0707-source/waferlockCRM.git

### 已完成的核心功能
- ✅ 首頁整合：案件大池（客訴單列表）+ 客戶導向搜尋（候選清單→完整 CRM 詳情）
- ✅ 客編（WF）以客戶為中心：案件發生時即發客編，安裝全程歸屬同一客編
- ✅ 派工處理中心：安裝 / 報修 / 客訴 合併為單一頁籤（子分頁切換）
- ✅ 工務時刻表：分區（北/中/南）+ 13 格時段，維修佔 1 格、安裝佔 2 格
- ✅ 使用者登入系統：帳密登入、視角權限（admin 可切換）、姓名浮水印
- ✅ 案件異動紀錄「**寫入**」：派工/結案/建單/排程/施工/驗收 皆記錄操作者
- ✅ 案件異動紀錄「**顯示**」：案件詳情列出 時間・動作・操作者
- ✅ 工務時刻表：格子顯示案件編號，點擊滑出案件詳情
- ✅ 客編中心化：客編唯一鍵＝「地址+姓名」（電話非唯一），可一鍵依地址整理歸戶

### 📋 副總交辦（主管 MD）完成度盤點

> 需求來源：[`supabase_schema_v2.sql`](supabase_schema_v2.sql) 內標記的 MD 1~6 / 盲點 1~5 / 決策。
> 多數項目「資料表已建好，但前端 UI 未接」。

**已完成**
- ✅ MD 4 — 工班分區派工調度（分區 + 13 格工務時刻表）
- ✅ 決策 2 — 全新工務端（師傅端：到場/施工/簽名照片回報）
- ✅ 結案不保固註記（保內/保外/保內不保固＋原因）
- ✅ 收費品項（數量×單價＋贈品）
- ✅ MD 6 — LINE OA 綁定（LIFF）

**遺漏（資料表 ✅／前端 UI ❌）**
| 需求 | 前端 |
|------|:---:|
| MD 1 — B2B 分段安裝/分段請款（`projects`/`billings`）| ❌ |
| MD 2 — SN 掃碼登錄＋出貨庫防呆＋消費者掃 QR（`devices`/`shipment_stock`）| ❌（師傅端無掃碼）|
| MD 3 — 報修身份（屋主/家人/租客＋租客高風險需房東確認）| ❌ |
| MD 5 — CRM 集團→建案階層 ＋ 主從帳號 | 🟡 階層瀏覽＋客戶歸屬建案已完成；主從帳號未做 |
| 盲點 1 — 出貨庫防呆（SN 登錄比對）| ❌ |
| 盲點 3 — SLA 緊急分級（normal/high/critical 紅色警戒）| 🟡 僅 hiRisk→high 寫入，無完整分級 |
| 盲點 4 — 無 SN 舊品動態補登 | ❌ |
| 盲點 5 — 一鎖多人授權（租客剔除）| ❌ |
| 決策 3 — ERP 串接模擬（`erp_sync`）| ❌ |
| 決策 5 — PDPA 同意流程（`consents`）| ❌ |
| 多媒體報修錄影上傳（`repairs.videos`）| ❌ |
| 門外報修外觀照片比對庫 | ❌（資料表亦未建）|

> 建議優先：MD 2（SN 掃碼登錄）、MD 5（集團→建案階層）最貼近核心營運。

### 🟡 未來可改善（非阻塞，依需求排）
- 異動紀錄缺口：改客戶資料（`saveEdit`）、改排程/移動時段 未必都有寫 history。
- 體驗：案件大池缺日期篩選/排序/分頁；搜尋為前端全量過濾；缺「我的待辦」視角。
- 效能：Realtime 每次全量重拉整個 DB；React 用瀏覽器即時編譯（development build）。
- 安全（MVP 暫緩）：密碼明文、RLS 全開。

### 風險 / 待收斂（上線前）
- 🔴 `crm_users.pin` 密碼明文（預設皆 `1234`）→ 需改 hash。
- 🔴 RLS 全開（`using(true)`）→ 登入只是前端閘門，非後端權限控管。

---

## 📍 目前狀態快照（2026-06-25 更新）

### 近期已完成（本輪迭代）

| 功能 | 狀態 | commit |
|------|:---:|--------|
| WorkOrderForm 新建工單底部固定送出按鈕 | ✅ | f88fd51 |
| 維修排程 / 安裝排程 fullsheet 底部加確認按鈕 | ✅ | f88fd51 |
| Hash URL Deep Linking（工單/客戶直接連結） | ✅ | f88fd51 |
| 移除工單表單預約時間欄位（已整合進排程畫面） | ✅ | 774898e |
| 新增客戶主檔後直接跳轉 CRM 詳情 bug fix | ✅ | b16e037 |
| CRM 主檔統一觸發工單 + 移除派工頁面 | ✅ | a0bcf21 |

### CRM 整體功能完成度

- ✅ 首頁整合：案件大池 + 客戶導向搜尋
- ✅ 客編（WF）以「地址+姓名」為唯一鍵，不靠電話
- ✅ 工務時刻表：分區 + 13 格時段（師傅排程視圖）
- ✅ 工單統一從 CRM 客戶主檔觸發：安裝 / 維修 / 完工驗收 / 保固登錄
- ✅ 集團→建案階層（GroupProjectPage + 客戶歸屬建案）
- ✅ 使用者登入 + 視角權限 + 案件異動紀錄
- ✅ LINE OA 推播 / LIFF 綁定 / 師傅端 ETA 回推
- ✅ Hash URL Deep Linking：每個工單/客戶可分享直接連結

### 🔴 待釐清後再做（B2B / ERP / 內部討論中）

> 已製作「WAFERLOCK CRM 系統規劃確認清單.docx」（21 題），等待內部討論結果。

| 項目 | 狀態 |
|------|------|
| B2B 建商合作案流程（批量工單、品管驗收、月結請款） | ⏸️ 等內部確認 |
| B2B 門廠合作案流程（LIFF 回報 or Google Form） | ⏸️ 等內部確認 |
| 驗收 Modal 加開票資訊（三聯/電子發票） | ⏸️ 等 ERP 討論 |
| TipTop ERP 串接（EasyFlow API 授權確認中） | ⏸️ 等主管確認 |
| `installType` 欄位（b2c / b2b_builder / b2b_door） | ⏸️ 等 B2B 確認後才做 |

---

## 🗂️ 未發布（Unreleased）

_尚未進入正式版本的改動寫在這裡，發布時再整理成版本。_

- 2026-06-25｜根治 CRM 開頁慢：施工/簽收照片改存 Supabase Storage。（Claude）
  - 🔴 **前置（需人工，已完成）：在 Supabase SQL Editor 執行 [`supabase_storage.sql`](supabase_storage.sql)** 建 `work-photos` public bucket＋讀寫 policy。
  - 問題定位：開 CRM 慢與連線無關，是照片 base64 塞進資料列——`repairs` 11 筆=12.9MB、`installs` 9 筆=8.1MB，`loadFromSupabase` 一次 `select('*')` 拉 **21MB**、耗時 **6.9 秒**；且 CRM 根本沒顯示這些照片（只寫不讀），等於每次白拉。
  - 修法：照片改存 Storage，資料表 `photos` 只留 public URL 陣列。兩檔各加共用 `uploadPhotos(files,folder)`（上傳 `work-photos/<repairs|installs>/<id>/...`，回傳 URL）。`waferlock_crm.html` `PhotoUpload` 改上傳 Storage（含「上傳中」狀態）、`WorkOrderModal` 傳 folder；`waferlock_tech.html` 維修 `onPhotos`、安裝上傳兩處同改。`signature` 簽名（全表 25–35KB）依計畫暫留 base64（影響極小）。
  - 資料層/載入不需改：`photos` 仍是陣列、`select('*')` 自然瘦身。
  - 既有照片一次性遷移：base64→Storage→URL（repairs 5 張、installs 4 張，0 失敗，冪等可重跑）；遷移前已備份至本機 `_photo_backup/`（已加 `.gitignore`，不進版控）。
  - ✅ **端到端驗證（正式 Supabase）**：上傳測試取得可開啟 public URL（200/image/png）；遷移後重量測 `loadFromSupabase` **6939ms→328ms**、總下載 **21MB→127KB**（repairs 49KB、installs 32KB）、無 base64 殘留；CRM 與師傅端 `uploadPhotos` 皆實測上傳成功；測試物件已清除、正式環境無殘渣。
  - 影響檔案：`waferlock_crm.html`、`waferlock_tech.html`、新增 `supabase_storage.sql`、新增 `.gitignore`。

- 2026-06-25｜維修/安裝案件「專屬網址」產生端補齊（deep link 缺口）。（Claude）
  - 問題：Hash deep link 的「解析端」早已支援 `#repair/R-xxx`、`#install/IW-xxx`（開啟連結能定位＋高亮案件），但「產生端」缺漏——系統內唯一更新網址處只有「選中客戶→`#crm/WF...`」，導致點開維修案件排程時網址仍停在客戶層級，使用者**無從取得案件專屬網址**。
  - 修法：`CRMPage` 新增單一 `useEffect`，監聽 `dispatchRepair`/`schedInstall`/`sel`——開啟維修排程→網址自動變 `#repair/<id>`、開啟安裝排程→`#install/<id>`、關閉後還原成 `#crm/<wfId>`。一個 effect 涵蓋開啟/關閉/還原，不需在 5～6 個關閉點各別處理。
  - ✅ 本機驗證：開維修排程→網址自動變 `#repair/R202606250003`（與排程畫面案件一致），關閉→還原 `#crm/WF62247000`；無 console 錯誤。安裝側為同 effect 對稱分支（當下無可排程安裝單可即時點測，未建假資料污染正式環境）。
  - ⚠️ 已知缺口：已結案案件無排程按鈕→目前仍只有客戶網址；若需「每個案件（含已結案）都能複製連結」可後續在案件卡片加「🔗 複製連結」鈕。
  - 影響檔案：`waferlock_crm.html`。

- 2026-06-25｜UI 修正：送出按鈕被 NavBar 遮蔽問題（三處）。（Claude）
  - `WorkOrderForm`（新建工單 modal）：改為 flex column 結構，表單內容可捲動，「取消 / 建立工單」按鈕固定在 modal 底部，上方加分隔線。
  - 維修排程派工 fullsheet：底部 `fs-body` 末端新增「取消 / 確認派工＋推播」重複按鈕（`fs-top` 頂部按鈕被 NavBar z-index 蓋住）。
  - 安裝排程 fullsheet：同上，底部新增「取消 / 確認排程」重複按鈕。
  - 影響檔案：`waferlock_crm.html`。

- 2026-06-25｜Hash URL Deep Linking：每個工單/客戶可分享直接連結。（Claude）
  - URL 格式：`#crm/WF12345678`（客戶主檔）、`#repair/R-xxx`（維修工單）、`#install/IW-xxx`（安裝工單）。
  - 登入後 + db 載入後自動解析 hash，找到對應工單/客戶 → 跳 CRM + 高亮工單卡片（藍框，3 秒後消失，自動捲動定位）。
  - 選中客戶時 `window.history.replaceState` 更新 URL hash；切換 NavBar 頁籤時清除 hash。
  - 影響檔案：`waferlock_crm.html`（App state、Context、CRMPage、NavBar）。

- 2026-06-25｜師傅端「定位回推抵達時間（ETA）」差異化。（Claude）
  - 🔴 **下一步（待做，需人工）：在 Supabase SQL Editor 執行 [`supabase_eta.sql`](supabase_eta.sql)**（`customers` 加 `lat`/`lng`；`repairs` 加 `eta_at`/`eta_text`/`accepted_at`/`arrived_at`/`est_distance_km`/`eta_source`）。未執行前接案寫 ETA 會在 sync 失敗。
  - 差異化核心：維修接案不再只推罐頭訊息，改為**接案當下讀師傅 GPS（限當次、只存距離不存座標）→ OSM Nominatim 把客戶地址轉座標（快取 `customers.lat/lng`）→ Haversine 算距離 → 用該師傅過去「接案→抵達」實際時程校準（min/km 中位數，冷啟動 fallback 市區車速）→ 回推時間範圍 ETA**，推播給客戶「預計今天 14:05–14:25 抵達」。
  - 退路：抓不到 GPS 或地址無法轉座標 → 跳手選時間範圍 sheet（+30/+60 分等），`eta_source='manual'`、不採計校準，仍能推播。
  - ⚠️ **OSM 台灣地址限制與修正**：實測發現 Nominatim 對「含門牌號」的台灣地址幾乎都回 NO_RESULT。新增 `addrVariants` 逐級降級鏈（① 去號保留路/段 ② 砍到 路/街/段 ③ 縣市+區 中心），實測 4 個真實地址 3 個拿到街道級、1 個退區級座標——配個人化校準精度已足夠。
  - 師傅端（`waferlock_tech.html`）：新增 `haversineKm`/`geocodeCustomer`/`getWorkerMinPerKm`/`estimateEta`/`manualEta` 純函式；改寫 `accept` 為「讀定位→估算→確認 sheet（可微調）→寫入」；`submitReport` 結案時補寫 `arrived_at`（校準閉環）；進行中卡片顯示「🚗 預計 … 抵達」。
  - CRM 端（`waferlock_crm.html`）：`dbToRepair`/`repairToDb` 補 6 個 ETA 欄位映射；維修卡片 `dispatched` 時於「⏰ 預約」旁顯示「🚗 預計抵達」。
  - 校準閉環權衡：以「結案時戳」當實際抵達時間（略高估耗時），校準層會吸收系統性偏差；要更準可後續加「📍 標記到場」按鈕（列為可選）。
  - ✅ 本機預覽驗證：師傅端所有函式正確定義、登入頁渲染、Haversine（台北→高雄 ≈297km）、`estimateEta`/`manualEta` 範圍正確、ETA sheet 渲染＋微調（+20 分位移正確）＋gps/manual 快捷鈕差異正確；CRM Babel 編譯通過、登入頁正常。
  - ✅ **正式環境（Supabase）端到端驗證**：6 個 `repairs` 新欄位＋`customers.lat/lng` 確認可讀寫；真實 OSM geocode（台中北屯崇德二路一段→24.176,120.695）→ Haversine（火車站→該址 4.48km）→ 校準（W1 無歷史回 fallback 2.4 min/km）→ ETA 範圍（10:01–10:21）→ 寫入測試工單 → 讀回 `eta_text`/`est_distance_km`/`eta_source` 皆正確 → 客戶 lat/lng 快取寫入可落地。**測試工單已刪除、客戶快取已還原，正式環境無殘留。**
  - 影響檔案：新增 `supabase_eta.sql`；`waferlock_tech.html`、`waferlock_crm.html`。
  - ⚠️ **2026-06-25 缺口修補（Claude）**：稽核時發現此功能雖記錄「已完成」，但 `waferlock_tech.html`（師傅端 136 行）與 `supabase_eta.sql` **先前漏未進版控**——只有 CRM 端顯示邏輯被夾帶部署，師傅端寫入邏輯沒上線（線上接案不寫 ETA → CRM 永遠顯示不出）。已補提交並 push（`bca74c0`），重新本機驗證純函式通過。教訓：功能完成後須確認**所有影響檔案都已 commit**，不能只看單一檔。

- 2026-06-23｜MD 5 集團→建案階層（資料）：新增 [`supabase_md5_seed.sql`](supabase_md5_seed.sql)，含 `groups`/`projects` 建表＋10 間真實台灣建商×各 10 個真實建案（共 100，公開資料查證）。
  - ✅ **SQL 已於正式 Supabase 執行並驗證**（`groups` 10 筆、`projects` 100 筆，每集團各 10 筆）；`customers` 已加 `group_id`/`project_id` 欄位。
- 2026-06-23｜MD 5 集團→建案階層（前端 UI）：在 `waferlock_crm.html` 接上資料層已完成的階層。（Claude）
  - 資料層：`loadFromSupabase` 載入 `groups`/`projects`；新增 `dbToGroup`/`dbToProject`；`custToDb`/`dbToCust` 補 `group_id`/`project_id`（沿用既有同步模式，groups/projects 為唯讀參考資料不在前端編輯）。
  - 新頁「🏢 集團/建案」（`GroupProjectPage`）：三層瀏覽 集團 → 建案 → 建案詳情；建案詳情列出已歸屬客戶與其案件（報修/客訴），可一鍵跳完整 CRM。
  - 客戶詳情頁新增「🏢 歸屬建案」按鈕與 modal（集團→建案連動下拉），寫入客戶 `groupId`/`projectId` 並補「建案客戶」標籤；詳情頁顯示目前歸屬。
  - 歸屬層級放在「客戶」，案件透過所屬客戶歸屬（避免動 `repairs`/`complaints` 無此欄位）。
  - ✅ 已在本機預覽驗證：階層瀏覽正確、歸屬寫入正式 Supabase 成功、建案詳情正確顯示該客戶與案件；驗證用的測試歸屬已還原。
  - 影響檔案：`waferlock_crm.html`（資料層、`CRMPage`、新增 `GroupProjectPage`、`App` 導覽與路由）。
- 2026-06-24｜統一「維修工單＋安裝工單」為一個版本（比照 KingNet 約工單）。（Claude）
  - 🔴 **下一步（待做，需人工）：在 Supabase SQL Editor 執行 [`supabase_workorder_v3.sql`](supabase_workorder_v3.sql)** 為 `installs`/`repairs` 加欄；未執行前統一表單建單會在 sync 階段失敗（前端會建立但無法寫回雲端）。
  - 新增共用元件 `WorkOrderForm`：單一「＋ 新增工單」表單，第一欄選 安裝／維修，依類別動態顯示專屬欄位；送出時仍寫回既有 `installs`/`repairs` 雙表（不做資料遷移）。
  - 兩入口共用此表單：`InstallPage` 的新增鈕（改名「＋ 新增工單（安裝／維修）」）、`CRMPage` 客戶詳情「🔧 新增派工」（鎖定為維修＋鎖定客戶）。
  - 新增欄位（installs/repairs 同步加）：承租身份(MD3 屋主/家人/租客＋租客需房東確認勾選)、來源通路、社區特殊需求、郵遞區號、工單類別細項、台數、預估工時、時間範圍(早/午/晚)、轟炸戶旗標、通用備註。
  - 可變工時：工時可填小數(如 1.5)，排程實際佔格＝`Math.ceil(工時)`，沿用既有 `bookSlots` 與整點 13 格衝突檢查（不改 bookings 結構）；`InstallPage.doSchedule`、`RepairPage.doDispatch` 已改用之。
  - `ChargeTable` 收費品項改「預設下拉(`CHARGE_PRESETS`)＋仍可手打」；維修派工註記區加「轟炸戶」。
  - 移除被取代的死碼：`InstallPage.createOrder`/`f` 狀態、`CRMPage.createCase`/`caseForm`。
  - ✅ 本機預覽驗證：頁面無編譯錯誤；統一表單從兩入口皆可開；安裝/維修類別切換正確顯示對應欄位；租客顯示房東確認提示；CRM 入口鎖定維修＋帶出客戶。
  - ✅ **已執行 `supabase_workorder_v3.sql` 並端到端驗證**：安裝/維修各建一張，新欄位（zip/work_hours/qty/tenant_type/landlord_confirmed…）皆正確寫入正式 Supabase；客編識別正常；測試資料已清除。
  - 影響檔案：`waferlock_crm.html`、新增 `supabase_workorder_v3.sql`。
- 2026-06-24｜地址選取畫面（中華郵政風格）＋ 工單收費品項 ＋ 第一頁排程。（Claude）
  - 🔴 **下一步（待做，需人工）：在 Supabase SQL Editor 執行 [`supabase_workorder_v4.sql`](supabase_workorder_v4.sql)**（`customers` 加 `zip`、`installs` 加 `charges`/`charge_total`；repairs 既有）。未執行前，帶收費品項的安裝單或客戶 zip 會在 sync 失敗。
  - 新增 `AddressPicker` 元件＋內嵌全台 `TW_ZIP3`（縣市→鄉鎮→3碼，中華郵政開放資料）：選縣市→鄉鎮自動帶郵遞區號與基底地址，再自行輸入 路/段/巷、棟(選填)、號、樓(選填)、之(選填)，確認後整包帶回。**地址輸入處全面改用**：統一工單 `WorkOrderForm`、CRM「編輯客戶資料」。移除原本分開的郵遞區號/地址/樓棟/戶號欄位。
  - `WorkOrderForm` 下方新增「🛒 購買/收費品項」（沿用升級後的 `ChargeTable`：預設品項下拉＋手打、數量×單價＋贈品＋合計），安裝+維修建單即存（`charges`/`chargeTotal`）。
  - 第一頁「預約時間」欄＋「📅 開啟排程畫面」可選：直接選區域/師傅/時段，建單時一併佔格（`ceil(工時)` 格，衝突則中止）；未開則僅建單、事後再排。
  - 資料層：`installs` 加 `charges`/`charge_total` 映射；`customers` 加 `zip` 映射。
  - ✅ **已執行 v4 SQL 並端到端驗證**：建安裝單實測，`address`/`zip`(806/407)、`charges`(更換鋰電池×2=1000)/`charge_total`、客戶 `zip` 皆正確寫入正式 Supabase；測試資料已清除。
  - 修正：工單建立「新客戶」時一併寫入客戶 `zip`（先前只寫到工單、客戶 zip 為 null）。
  - 影響檔案：`waferlock_crm.html`、新增 `supabase_workorder_v4.sql`。
- 2026-06-24｜品項二層選單改造：安裝（全型號分系列）＋ 維修 ＋ 移除「安裝產品」欄位。（Claude）
  - 新增常數 `INSTALL_MODELS`：D系列(D300/D303/D310/D312)、C系列(C210/C760)、L3系列(16款含L375/L775雙版)、L6系列(L600)、L7系列(L701)、L9系列(L900/L901)、R400，共 28 款，來源：WAFERLOCK全型號知識庫.xlsx。
  - `ChargeTable` 品項欄改為二層：第一層選「安裝/維修」，第二層依類別展開：安裝→型號以系列 `<optgroup>` 分類；維修→保留原 8 個品項（含手打覆寫）。
  - `WorkOrderForm`：移除「安裝產品」`<select>` 欄（原只列 5 款舊型號）；改以 `useEffect` 監聽品項列，選「安裝」類別並挑型號後自動回填 `productId`（供保固計算 fallback `PRODUCTS[productId]?.warranty||36`）。
  - 目的：對應 ERP 商品編號，未來可直接比對。
  - ✅ 本機預覽驗證：類別欄 [— 類別 —, 安裝, 維修]；安裝→28 款 7 系列 optgroup；維修→8 品項；「安裝產品」label 已消失；console 無錯誤。
  - 影響檔案：`waferlock_crm.html`（`INSTALL_MODELS` 常數、`ChargeTable`、`WorkOrderForm`）。
- 2026-06-24｜客編流程修正：先建客戶主檔 ＋ 安裝工單建案聯動。（Claude）
  - **盲點修正**：客編不應是工單的副產品，客戶應先於工單存在；B2B 安裝工單建單時應能指定建案歸屬。
  - `WorkOrderForm` 購買通路選「建案配置」時，聯動顯示「所屬集團」和「所屬建案」下拉（10 間真實建商 × 各 10 建案），送出時自動寫入新/既有客戶的 `groupId`/`projectId`。
  - 新增 `CreateCustomerModal` 元件：客服可不依附工單，直接從首頁「＋ 新增客戶主檔」建立客戶（姓名＋地址必填，購買通路可選）；建案配置通路亦支援集團/建案聯動；查重機制（`custKey`）：已存在則跳至 CRM 詳情不重建。
  - 新客戶 tag 為 `['已登錄']`（區別於工單自動建立的 `待安裝`/`待整理`）。
  - `custToDb`/`syncToSupabase` 已含 `group_id`/`project_id` 映射，無需另改資料層。
  - ✅ 本機驗證：首頁「＋ 新增客戶主檔」modal 正確開啟；選建案配置→集團/建案下拉出現（遠雄建設、興富發建設…）；安裝工單同樣聯動；console 無錯誤。
  - 影響檔案：`waferlock_crm.html`（新增 `CreateCustomerModal`、`HomePage`、`WorkOrderForm`）。
- 2026-06-24｜CRM 主檔統一觸發工單 ＋ 移除派工頁面。（Claude）
  - **A. WorkOrderForm 解除類別卡控**：移除 `initialCategory="repair"` 及 `disabled` 邏輯，CRMPage 開啟的工單 modal 可自由選安裝或維修。
  - **B. CRMPage 加入工單推進操作**：新增維修排程/結案按鈕；新增安裝工單區塊（排程、已到場、施工完成、完工驗收+保固登錄、無法安裝）；內嵌 SlotPicker 半頁排程介面及 WorkOrderModal 驗收電子工單。
  - **C. 編輯客戶資料 modal 補建案聯動**：購買通路改為「建案配置」時顯示集團/建案下拉，儲存時同步寫入 `groupId`/`projectId`。
  - **D. 移除派工處理頁面**：刪除 `RepairPage`、`InstallPage`、`ComplaintPage`、`DispatchCenter` 元件；移除 NavBar「🛠️ 派工處理」按鈕及 `page==='dispatch'` 路由；移除已無用的 `pendingCnt`/`installPending`/`complaintPending`/`urgentCnt` 計數器。
  - ✅ 本機驗證：NavBar 剩 3 頁簽（首頁/工務時刻表/集團建案）；CRMPage 顯示「🛠️ 安裝工單（1 筆）」及「🔬 完工驗收」按鈕；console 無錯誤。
  - 影響檔案：`waferlock_crm.html`（移除約 530 行舊頁面；修改 `WorkOrderForm`、`CRMPage`、`App`）。

---

## 📦 版本歷程（新→舊）

### v0.8 — 客編識別改為「地址+姓名」（2026-06-23）
- 領域知識更正：**電話非唯一**（同號可能多住所：房東/房仲/包租代管），客編唯一鍵改為**正規化地址 + 姓名**
- 新增 `normAddr`/`custKey` 工具（全形轉半形、去空白、臺→台）
- `createOrder`（安裝建單）：地址改必填，以「地址+姓名」查既有客戶（不再用電話）
- 「整理客編」改為依「地址+姓名」歸戶，**冪等**：判定「案件地址+姓名與其客編對不上」才需整理；修正先前用電話誤併者；清除無案件引用的待整理孤兒客編
- `CaseDetailPanel` 聚合改以客編(wfId)為主，電話僅在無客編時輔助（避免同號不同住所混在一起）
- 影響檔案：`waferlock_crm.html`（全域工具、`InstallPage.createOrder`、`HomePage.runBackfill`、`CaseDetailPanel`）

### v0.7 — 客編中心化收尾：未歸戶案件整理（2026-06-23）
- 首頁新增管理員專用按鈕「🔗 整理未歸戶案件（N）」：把沒有客編的案件依電話歸到既有客戶，無對應則新建客編（tag `待整理`）
- 同電話多筆案件會集約到同一客編；已存在客戶則直接沿用、不重建
- 影響檔案：`waferlock_crm.html`（`HomePage`）
- 補充：報修/客訴從 CRM 建立時本就帶客編（`createCase`/`createComplaint`），新案件無此問題

### v0.6 — 工務時刻表點擊查看 + 異動紀錄顯示（2026-06-22）
- 工務時刻表格子顯示案件編號（R…/IW…），點格子 → 右側滑出案件詳情
- 案件詳情新增「📝 本案異動紀錄」區，列出 時間・動作・操作者（repair/install 取 history，complaint 取 timeline）
- 影響檔案：`waferlock_crm.html`（`SchedulePage`、`CaseDetailPanel`）

### v0.5 — 使用者登入系統（2026-06-22）
- `aa8c595` 新增使用者登入系統：帳密登入 + 視角權限 + 姓名浮水印 + 案件異動紀錄（寫入）
- 對應 SQL：`supabase_users_history.sql`（已於正式 Supabase 執行）

### v0.4 — CRM 首頁整合與客編中心化（2026-06-22）
- `1cdad27` 合併 安裝/報修/客訴 為單一「派工處理」頁籤
- `a14487a` 客編改為案件發生時即發行（安裝全程歸屬同一客編）
- `28e77fb` 移除重複的 CRM 客戶與場域搜尋分頁，整合至首頁
- `a093261` 首頁搜尋改為候選客戶清單→跳轉完整 CRM 詳情頁
- `24ac624` 首頁搜尋改為客戶導向（輸入電話→直接進客戶完整 CRM）
- `0b6b9f1` CRM 首頁整合案件大池（客訴單列表）+ 客戶歷程詳情面板

### v0.3 — 派工 / 安裝工單 / 工務時刻表（2026-06-21）
- `d3529fa` 師傅端加入安裝工單支援（到場/施工完成/照片簽名回報）
- `3067193` 工務時刻表改為 X軸=師傅、Y軸=時間
- `7c484f6` 全頁派工/安裝工單 + 分區排程(13格滿工) + 工務時刻表 + 不保固註記 + 收費品項
- 對應 SQL：`supabase_dispatch.sql`

### v0.2 — LINE 整合與師傅端（2026-06-18）
- `90fc0e4` index.html 加師傅端入口卡 + rich menu 圖
- `bf64d32` LINE 訊息加 sender 標示來源（AI/真人/客服）
- `bb03280` 新增獨立師傅端 + 案件編號規則（R/C+YYYYMMDD+流水）
- `ddd4962`/`78bfe9f`/`dd4674f` LINE 圖文選單（可切換分頁、規格修正）
- `3d7c0e2` 修正推播了但收件匣沒紀錄（以 lineUid 對應對話寫入）
- `5f224e4`/`41dfea7` CRM / 客服回覆透過 line-push 推回真實 LINE
- `d570dfa` 新增 LINE Messaging Edge Functions（webhook / push）
- `83059af`/`88382cd` LIFF 三合一（綁定/保固查詢/報修）+ Supabase schema v2 草案

### v0.1 — 基礎建設（2026-06-17）
- `d352378` 新增 index.html 入口頁
- `81bc04d` 遷移至 Supabase 資料庫

---

## 🛠️ 協作守則（給所有協作者，含 AI）

1. **改動前**：先讀本文件「目前狀態」與「未發布」，確認沒有人正在做同一塊。
2. **改動中**：遵守 `CLAUDE.md` 的編程契約（外科手術式修改、約定優先、顯性失敗）。
3. **改動後**：在「未發布」補一筆，格式：`- 日期｜做了什麼｜影響的檔案/功能｜（你的名字）`。
4. **commit**：訊息用繁體中文描述「做了什麼」。
5. **發布**：把「未發布」的內容整理成新版本區塊，push 到 `main`（即自動部署正式環境）。
