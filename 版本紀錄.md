# WAFERLOCK CRM 版本紀錄（CHANGELOG）

> ⚠️ **協作守則：動手改任何東西之前，請先讀完本文件。**
> 改完之後，**務必在最上方「未發布」區補一筆**（日期、做了什麼、誰做的、影響範圍），再 commit。
> 這份文件是「目前做到哪、還缺什麼」的單一真實來源（single source of truth）。
> 詳細的系統架構請另見 [`專案總覽.md`](專案總覽.md)。

---

## 📍 目前狀態快照（2026-06-23）

- **主檔**：`waferlock_crm.html`（單一 HTML，React CDN + Babel，無建置工具）
- **後端**：Supabase（URL/anon key 硬編碼於前端；RLS 目前全開）
- **線上**：GitHub Pages，主分支 `main` 一 push 即自動部署
- **遠端**：https://github.com/fishchen0707-source/waferlockCRM.git

### 已完成的核心功能
- ✅ 首頁整合：案件大池（客訴單列表）+ 客戶導向搜尋（候選清單→完整 CRM 詳情）
- ✅ 客編（WF）以客戶為中心：案件發生時即發客編，安裝全程歸屬同一客編
- ✅ 派工處理中心：安裝 / 報修 / 客訴 合併為單一頁籤（子分頁切換）
- ✅ 工務時刻表：分區（北/中/南）+ 13 格時段，維修佔 1 格、安裝佔 2 格
- ✅ 使用者登入系統：帳密登入、視角權限（admin 可切換）、姓名浮水印
- ✅ 案件異動紀錄「**寫入**」：派工/結案/建單/排程/施工/驗收 皆記錄操作者
- ✅ 案件異動紀錄「**顯示**」：案件詳情列出 時間・動作・操作者
- ✅ 工務時刻表：格子顯示案件編號，點擊滑出案件詳情
- ✅ 客編中心化：客編唯一鍵＝「地址+姓名」（電話非唯一），可一鍵依地址整理歸戶

### 📋 副總交辦（主管 MD）完成度盤點

> 需求來源：[`supabase_schema_v2.sql`](sql/supabase_schema_v2.sql) 內標記的 MD 1~6 / 盲點 1~5 / 決策。
> 多數項目「資料表已建好，但前端 UI 未接」。

**已完成**
- ✅ MD 4 — 工班分區派工調度（分區 + 13 格工務時刻表）
- ✅ 決策 2 — 全新工務端（師傅端：到場/施工/簽名照片回報）
- ✅ 結案不保固註記（保內/保外/保內不保固＋原因）
- ✅ 收費品項（數量×單價＋贈品）
- ✅ MD 6 — LINE OA 綁定（LIFF）

**遺漏（資料表 ✅／前端 UI ❌）**
| 需求 | 前端 |
|------|:---:|
| MD 1 — B2B 分段安裝/分段請款（`projects`/`billings`）| ❌ |
| MD 2 — SN 掃碼登錄＋出貨庫防呆＋消費者掃 QR（`devices`/`shipment_stock`）| ❌（師傅端無掃碼）|
| MD 3 — 報修身份（屋主/家人/租客＋租客高風險需房東確認）| ❌ |
| MD 5 — CRM 集團→建案階層 ＋ 主從帳號 | 🟡 階層瀏覽＋客戶歸屬建案已完成；主從帳號未做 |
| 盲點 1 — 出貨庫防呆（SN 登錄比對）| ❌ |
| 盲點 3 — SLA 緊急分級（normal/high/critical 紅色警戒）| 🟡 僅 hiRisk→high 寫入，無完整分級 |
| 盲點 4 — 無 SN 舊品動態補登 | ❌ |
| 盲點 5 — 一鎖多人授權（租客剔除）| ❌ |
| 決策 3 — ERP 串接模擬（`erp_sync`）| ❌ |
| 決策 5 — PDPA 同意流程（`consents`）| ❌ |
| 多媒體報修錄影上傳（`repairs.videos`）| ❌ |
| 門外報修外觀照片比對庫 | ❌（資料表亦未建）|

> 建議優先：MD 2（SN 掃碼登錄）、MD 5（集團→建案階層）最貼近核心營運。

### 🟡 未來可改善（非阻塞，依需求排）
- 異動紀錄缺口：改客戶資料（`saveEdit`）、改排程/移動時段 未必都有寫 history。
- 體驗：案件大池缺日期篩選/排序/分頁；搜尋為前端全量過濾；缺「我的待辦」視角。
- 效能：Realtime 每次全量重拉整個 DB；React 用瀏覽器即時編譯（development build）。
- 安全（MVP 暫緩）：密碼明文、RLS 全開。

### 風險 / 待收斂（上線前）
- 🔴 `crm_users.pin` 密碼明文（預設皆 `1234`）→ 需改 hash。
- 🔴 RLS 全開（`using(true)`）→ 登入只是前端閘門，非後端權限控管。

---

## 📍 目前狀態快照（2026-06-26 更新）

### 近期已完成（本輪迭代，已全數推上正式環境）

| 功能 | 狀態 | commit |
|------|:---:|--------|
| Realtime 改增量更新，消除每次 DB 變動全量重拉（根治 egress 異常） | ✅ | 2a8b055 |
| 師傅端 pushCustomer 改從 conversations 找真實 LINE uid | ✅ | a4ec6bb |
| ✅ **端到端驗證**：師傅接案 ETA 推播正常送達 LINE App（R202606250007） | ✅ | — |
| 師傅端推播改雙軌：寫對話 ＋ 發真實 LINE | ✅ | 3ac6041 |
| 報修工單編號統一（LINE 端走 next_case_no） + 推播顯性化 | ✅ | dde7edc |
| 照片改存 Supabase Storage，根治 CRM 開頁慢（21MB→127KB） | ✅ | beba149 |
| 維修/安裝案件專屬網址產生端補齊（deep link 完整閉環） | ✅ | 185b62c |
| 師傅端 ETA 定位回推補提交（缺口修補） | ✅ | bca74c0 |
| WorkOrderForm / fullsheet 送出按鈕修正（NavBar 遮蔽） | ✅ | f88fd51 |
| Hash URL Deep Linking 基礎（工單/客戶直接連結） | ✅ | f88fd51 |
| 移除工單表單預約時間欄位 | ✅ | 774898e |
| 新增客戶主檔後直接跳轉 CRM 詳情 bug fix | ✅ | b16e037 |
| CRM 主檔統一觸發工單 + 移除派工頁面 | ✅ | a0bcf21 |

### CRM 整體功能完成度

- ✅ 首頁整合：案件大池 + 客戶導向搜尋
- ✅ 客編（WF）以「地址+姓名」為唯一鍵，不靠電話
- ✅ 工務時刻表：分區 + 13 格時段（師傅排程視圖）
- ✅ 工單統一從 CRM 客戶主檔觸發：安裝 / 維修 / 完工驗收 / 保固登錄
- ✅ 集團→建案階層（GroupProjectPage + 客戶歸屬建案）
- ✅ 使用者登入 + 視角權限 + 案件異動紀錄
- ✅ LINE OA 推播 / LIFF 綁定 / 師傅端 ETA 回推（雙軌：寫對話＋發 LINE App）
- ✅ Hash URL Deep Linking：工單/客戶可分享直接連結，開啟/關閉排程自動更新網址
- ✅ 照片存 Supabase Storage：CRM 載入從 6.9 秒降至 0.3 秒

### 🔴 待釐清後再做（B2B / ERP / 內部討論中）

> 已製作「WAFERLOCK CRM 系統規劃確認清單.docx」（21 題），等待內部討論結果。

| 項目 | 狀態 |
|------|------|
| B2B 建商合作案流程（批量工單、品管驗收、月結請款） | ⏸️ 等內部確認 |
| B2B 門廠合作案流程（LIFF 回報 or Google Form） | ⏸️ 等內部確認 |
| 驗收 Modal 加開票資訊（三聯/電子發票） | ⏸️ 等 ERP 討論 |
| TipTop ERP 串接（EasyFlow API 授權確認中） | ⏸️ 等主管確認 |
| `installType` 欄位（b2c / b2b_builder / b2b_door） | ⏸️ 等 B2B 確認後才做 |

---

## 🗂️ 未發布（Unreleased）

_尚未進入正式版本的改動寫在這裡，發布時再整理成版本。_

- 2026-07-16｜新增「新裝訂單流程盤點 Before/After」獨立網頁（可填寫儲存）。（Claude）
  - 背景：新裝流程（After）已上線，使用者要一份 Before/After 對照——Before 是現行人工流程（口述 10 步），每步附輸入框陸續補充現況與痛點（尤其「查庫存／ERP 出貨／師傅安排」三段模糊），作為之後調整流程的依據。
  - 新增 [`流程盤點_新裝訂單.html`](流程盤點_新裝訂單.html)（vanilla JS＋supabase-js CDN，比照 `liff-bind.html` 輕量模式，不動 CRM 主檔）：
    - 左欄 **Before 10 步**（LINE@ 進線→門樣式→訂購人資料→約定付款→查庫存⚠️→ERP 下單出貨⚠️→安排師傅⚠️→簽單回報→收款→發票保固），3 個模糊段以虛線框＋⚠️標記；每步兩個輸入框「📝 現況補充說明」＋「❓ 問題與待確認」。
    - 右欄 **After 8 步**（CRM 新流程唯讀對照），每張標注「↔ 對應 Before 步驟 N」。
    - 頁尾誠實列出 After 尚未涵蓋的缺口：門樣式蒐集／庫存查詢／匯款到帳確認／四種業務分岔。
    - 儲存：填寫人＋💾 儲存 → 打包 JSON upsert 至既有 `config` 表（key=`flow_audit_install_before`，**免 schema migration**）；載入自動回填；儲存失敗顯性 toast（檢查 `.error`）。
  - ✅ 驗證（本機 preview 連正式 Supabase）：渲染 10 步（3 模糊段）／20 輸入框／8 步 After／缺口框；填測試值儲存 → 重新整理 → 內容正確讀回；測試資料已清除復原；console 零錯誤。
  - 使用方式：部署後開 `https://<pages 網址>/流程盤點_新裝訂單.html`（未加入 index 入口頁，直連即可）。
  - 影響檔案：新增 `流程盤點_新裝訂單.html`。

- 2026-07-15｜新人手冊新增「情境教學篇」：情境一 — 客戶購買電子鎖全流程（`manual/`）。（Claude）
  - 背景：手冊原為功能導向（客服/派工/師傅各篇），新增情境導向教材——跟一張訂單走完三系統：LINE 購買諮詢 → 建檔建安裝單 → ERP 出貨閘門 → 排程派工 → 師傅安裝回報 → CRM 完工驗收＋保固登錄（SR 簽收單、保固自安裝日起算）→ 請款推進 → 住戶 LINE 保固查詢。8 站式，含 3 個考點（出貨閘門鎖排程、師傅端不產 SR、保固由驗收自動起算）與一條龍狀態複習表。
  - 新截圖 3 張（`shoot.py` 新增 `shoot_scenario`，沿用只開不送原則，零 DB 寫入）：`13-crm-install-case`（單張案件視圖，深連結 hash 導航）、`14-crm-verify-modal`（完工驗收 Modal，紅框標三步驟/收款/開票）、`27-line-warranty`（LINE 保固查詢，用 `add_init_script` 預塞 localStorage 綁定 WF61652509，未按綁定送出）。`annotate.py` 補 13/14 兩張標註。
  - ⚠️ 顯性跳過：「🚚 標記出貨」Modal 無法拍攝（DB 目前唯一安裝單已過出貨階段，無 pending 未出貨單），第 3 站以文字描述出貨閘門；`27-line-warranty` 示範帳號（陳先生）無安裝資料，畫面呈現「尚無保固資料」狀態——圖說已註明走完驗收後會變進度條。
  - ✅ 驗證：Playwright 實際渲染 `http://localhost:8765/manual/` 逐屏截圖目視（8 站、圖、表、側欄錨點皆正常），console 零錯誤、零載圖失敗；22 個 `img` 引用與 `fig` 區塊數量相符且檔案齊全。
  - 影響檔案：`manual/index.html`（側欄＋情境章節）、`manual/shoot.py`（新增 shoot_scenario）、`manual/annotate.py`（新增 2 組標註）、`manual/img/`（新增 3 張）。

- 2026-07-15｜新人操作手冊補圖完稿（`manual/index.html`）。（Claude）
  - 背景：手冊 lead 承諾「每一步都配上真實系統畫面」，但先前只嵌入 6 張截圖（`shoot.py` 已批次截好 25 張放 `img/`），多數步驟有文字無圖。
  - 本輪：把既有截圖補進對應段落，圖數 6 → 17。新增登入畫面（01）、客戶 LINE 端模擬（20）、收件匣列表（21）、0800 話後處理 ACW（26）、搜尋結果（03）、工務時刻表（08）、師傅端四頁籤（31）＋進行中卡片含 ETA（32）＋我的報表（35）、報表篇補齊師傅（10）與客服績效（12）兩張。
  - 未採用的 8 張為刻意取捨（02 首頁、05 歷史、06 舊版新增工單、09 集團建案、23/24 為 25/26 的前置狀態、30 師傅登入與 01 雷同、34 已完成空畫面）。
  - 只動 `manual/index.html`（純補 `<div class="fig">`，未改版面 CSS 與文案結構）；`shoot.py`/`annotate.py`/`img/` 皆未動。
  - ✅ 驗證：grep 比對 17 個 `img/*.png` 引用全部在 `img/` 存在、`class="fig"` 與 `<img>` 各 17 個數量相符。⚠️ 未在瀏覽器實際開頁目視（browser pane 擋 `file://`），版面渲染以既有 `.fig` 樣式沿用推定正常。

- 2026-07-07｜新裝訂單流程整頓：表單清欄 ＋ ERP 出貨段 ＋ 驗收開票請款。（Claude）
  - 背景：順一次新裝流程（各管道購買 → 建客戶主檔 → 安裝單 → ERP 出貨 → 派工 → 安裝 → 驗收 → 請款開票），並清掉安裝單上不該露出的資訊。未來一般/電商/實體店面/經銷商業務共用此流程。
  - **安裝單表單清欄**（維修單不受影響）：移除「風險旗標（⚠️高風險/🔧困難施工/💣轟炸戶）」「社區特殊需求」（兩者改為僅維修單顯示）與「指派鎖店師傅」（與排程派工師傅重複、易混淆；`submitInstall` 改寫 `locksmithId:null`，安裝卡片「🔑 負責師傅」列一併移除）。「承租身份＋房東確認」依決策保留。`CHANNELS` 補「🤝 經銷商」通路選項。
  - **ERP 出貨段**：不加新 status，改用欄位閘門——`installs` 新增 `shipment_no`/`shipped_date`；安裝卡片（pending）無出貨單號時顯示「🚚 待出貨」並鎖住「排程派工」（提示先完成 ERP 出貨），新增「🚚 標記出貨」Modal 填 TipTop 出貨單號＋日期（必填單號，寫 `pushHist`）；**已排程的舊單不回溯鎖定**（`workerId` 有值即不鎖）。
  - **驗收開票欄**：`WorkOrderModal`（安裝驗收＋維修結案共用）新增「🧾 開票資訊」——發票類型（電子/二聯/三聯）三選一，三聯必填 8 碼統編＋抬頭（前端驗證）；`finishInstallVerify`/`finishRepairClose` 寫入 `invoiceType/taxId/invoiceTitle`。發票實際由 TipTop 開立，CRM 只記錄。
  - **請款狀態**（先做安裝單）：`installs` 新增 `billing_status`，完工後卡片顯示「⏳ 待請款 → 📤 已請款 → 🧾 已開票」單向推進按鈕，每步寫異動紀錄；驗收完成時初始化為 `unbilled`。
  - **業務通路差異（一般/電商/實體/經銷商）**：使用者標記「待確認」——本輪不做流程分岔，僅以購買通路欄位做來源標記；發票代開（電商平台）/月結（經銷商）等分岔待與業務端確認後另案。
  - 🔴 **需人工執行**：到 Supabase SQL Editor 跑一次 [`sql/supabase_install_flow.sql`](sql/supabase_install_flow.sql)（installs 加 6 欄、repairs 加 3 欄，idempotent）。**未執行前**：標記出貨／驗收送出時 upsert 會因欄位不存在而失敗——因前一輪已把同步失敗顯性化，畫面會跳「⚠️ 資料同步失敗」toast 提醒，不會靜默遺失。
  - ✅ 驗證（本機 preview 連正式 Supabase，僅開表單未寫入）：安裝單表單三欄已消失、切維修單三欄仍在、經銷商選項存在；既有已排程安裝單（IW1783996454528）顯示「標記出貨」按鈕且排程未被鎖（不回溯正確）、鎖店師傅列已消失；console 零錯誤。⚠️ 未實測分支（無現成資料，邏輯已讀碼核對）：「pending 未排程單的排程鎖定」與「done 單的請款推進按鈕」，請於下一張真實新裝單流程中順手確認。
  - 影響檔案：`waferlock_crm.html`（WorkOrderForm/WorkOrderModal/renderInstallCard/CRMPage state 與函式/欄位轉換層/CHANNELS）、新增 `sql/supabase_install_flow.sql`。

- 2026-07-08｜語音客服 **G2b：口頭報修 AI 自動建維修單**。（Claude＋使用者）
  - `voicebot-tools` 新增 `create_repair(name,phone,address,issue,product)`：`next_case_no('R')` 取號 → 依**地址**找既有客戶沿用客編（客編唯一鍵=地址）、新客戶 `next_wf_id()` 建檔（`reg_type:'ai_voice'`、tags 待整理）→ INSERT `repairs`（`created_by:'AI語音客服'`）→ 工單掛回 `customers.repair_ids`。
  - `gemini-live-token`：加 `create_repair` 工具宣告＋【報修規則】（依序問齊稱呼/地址/電話/問題→複誦確認→才建單→念工單編號；沒問齊絕不建單）。
  - ✅ 實測（線上 Edge Function 直打）：建單成功、新客自動建檔、**同地址第二次報修沿用同客編不重複建檔**、`repair_ids` 正確累加。測試資料已清除。⚠️ curl 測試中文在 Windows 終端會亂碼寫入（工具編碼問題，瀏覽器前端 UTF-8 不受影響）。
  - ⚠️ 真機語音報修流程（AI 問答收集四項資訊→建單）待實測。
  - 影響檔案：`supabase/functions/voicebot-tools/index.ts`、`supabase/functions/gemini-live-token/index.ts`。

- 2026-07-07｜語音客服 **G2a：AI 可查資料庫回答維修進度**（function calling 端到端通）。（Claude＋使用者）
  - 新增 `supabase/functions/voicebot-tools/index.ts`：`get_case_status(phone)` 用電話查 `repairs`、狀態碼轉中文（dispatched→已派工給師傅等）、只回進度不回完整地址（隱私護欄）。
  - `gemini-live-token`：**tools 綁進 token 的 `bidiGenerateContentSetup.tools`**（實測 ephemeral token 會忽略前端 config 的 tools，必須綁 token 才生效）＋工具使用規則 `TOOL_RULES` 附加在人設之後。
  - 前端 `gemini-live-client.js`：收到 `message.toolCall.functionCalls` → 轉發 `voicebot-tools` → `sendToolResponse` 回結果，AI 再依結果回答；`call.html` 傳入 `toolsUrl`。
  - ✅ **端到端實測**（線上 token＋真實 DB）：問維修進度→AI 呼叫 `get_case_status(電話)`→查真實 `repairs`→口語回答真實進度（陳先生 0911111111：電子鎖、已派工、師傅7/3接案）；查無電話→AI 問是否要報修。voicebot-tools 直接 curl 也正確。
  - ⚠️ 前端瀏覽器 toolCall 迴圈**待真機測**（本機用 python 平行邏輯＋語法檢查驗證，未在真瀏覽器跑）。
  - ⚠️ 尚未做：G2b（`create_repair` 口頭報修建維修單）、更多查詢工具（保固/客戶資料）、G3（對話落地＋轉真人）。
  - 影響檔案：新增 `supabase/functions/voicebot-tools/index.ts`；改 `supabase/functions/gemini-live-token/index.ts`、`comm-voice/public/js/gemini-live-client.js`、`comm-voice/public/call.html`。

- 2026-07-07｜語音客服 **G1 真機驗證通過**＋改用 **gemini-3.1-flash-live-preview**（延遲~0.9s）＋**人設可在設定頁即時編輯**。（Claude＋使用者）
  - **部署上線**：本機裝 Supabase CLI（devDependency）部署 `gemini-live-token` Edge Function（原 npm SDK 版 bundle 失敗致 404，改純 Deno fetch 版才成功）；curl 實測簽發 200。真機（Edge/Chrome）端到端：AI 用 Leda 聲音**主動問候**、**雙向對話**、**barge-in 可打斷**皆通。
  - **排錯歷程**：① Chrome `NotReadableError`＝麥克風被佔用（非程式）；② 只聽到問候、講話無回應＝原 `onaudioprocess` 的 `catch{}` 吞掉上行送音訊錯誤＋native audio 連上後會等對方開口 → 加開場問候觸發＋上行 log；③ 延遲 5-8s → 調 VAD `silenceDurationMs`＋改 3.1 模型 → ~1s；④ barge-in 被環境音（音量0.009）誤觸發 → `startOfSpeechSensitivity` 改 LOW。
  - **模型 2.5-native-audio → 3.1-flash-live**：首音延遲 ~0.9s（native 約 1.5-2s），代價是 3.1 不支援情感對話（`enableAffectiveDialog` 會 1011 錯誤，已移除）；音色仍 Leda。
  - **人設外部化**：新增 `voicebot_settings` 表（`sql/supabase_voicebot_settings.sql`）＋設定頁 `comm-voice/public/voicebot-admin.html`（大文字框編輯 system_prompt/greeting、anon key upsert）；Edge Function 簽發前讀該表注入人設、讀不到用內建後備，並回傳 greeting 給前端當開場觸發。**改人設即時生效、下一通電話就套用，不用改 code**。
  - ⚠️ 設定頁未加密碼（網址私密）；access token 用完即請使用者 revoke。
  - ⚠️ **尚未做**：G2（讀資料庫回答＋建維修工單 function calling）、G3（對話落地 conversations＋轉真人信令閉環）。
  - 影響檔案：`supabase/functions/gemini-live-token/index.ts`、`comm-voice/public/js/gemini-live-client.js`、`comm-voice/public/call.html`、新增 `comm-voice/public/voicebot-admin.html`、`sql/supabase_voicebot_settings.sql`。

- 2026-07-07｜語音客服改走 **Gemini Live API**：G0 驗證通過（音色定 Leda）＋ G1 前端接通程式就緒（待部署真機驗證）。（Claude＋使用者）
  - **路線變更背景**：NVIDIA 免費 hosted TTS 實測連官方範例（官方 function-id＋en-US＋Aria voice）都精確 30 秒伺服器端 `failed to establish link to worker` 超時（client deadline 拉到 90 秒仍在 30 秒失敗＝NVCF 端無可路由 worker，非設定問題），穩定性不可靠。使用者決策改用正牌 Gemini Live API（付費但便宜、含免費層）。原 NVIDIA `stt-worker/voicebot.py` **保留不刪**；`docs/voicebot路線圖.md` 頂部已加路線變更註記。
  - **G0 驗證（本機 PoC，全過）**：模型 `gemini-2.5-flash-native-audio-preview-12-2025`。①金鑰／模型連線 ②中文語音回覆 ③**function calling**（問維修進度時模型自己呼叫 get_repair_status 假工具，證明 G2 讀資料庫/建工單架構可行）三項通過。試聽 5 種台灣親切女聲，**定案 Leda**；情感對話模式（enableAffectiveDialog）可用、減少 AI 感。ephemeral token 簽發驗證通過。
  - **費用試算**：音訊輸入 $3/1M tokens（≈$0.005/分）、輸出 $12/1M（≈$0.018/分），估每分鐘通話約 **NT$0.5~0.65**；月 2000 分鐘約 NT$960~1280。腔調為標準普通話（native audio 自然度高，靠 system prompt 要求台灣用語）。
  - **G1 程式（前端接通 Gemini Live）**：
    - 新增 `supabase/functions/gemini-live-token/index.ts`：簽發短效憑證（uses:1、30 分過期），把 model／Leda 音色／台灣客服 system prompt／情感對話全鎖在 token 的 `liveConnectConstraints`，**GEMINI_API_KEY 只留伺服器、不進前端、前端無法竄改人設**。
    - 新增 `comm-voice/public/js/gemini-live-client.js`：用 ephemeral token 直連 Gemini Live WS（不用自養 proxy 伺服器）、麥克風 16k PCM 上行、AI 24k PCM 下行排程播放、**原生 barge-in**（收到 interrupted 即停播，移除舊半雙工閉麥）、掛斷完整釋放麥克風 track。SDK 由 esm.sh 動態載入。舊 `voicebot-client.js` 保留不刪。
    - 改 `comm-voice/public/call.html`：接點換 gemini-live-client；**AudioContext 改在使用者點擊手勢內同步建立**（修 iOS/Safari suspended 全程無聲）；**AI 接通即 cancelWait**（修「50 秒計時器在 AI 對談中誤彈留言表單」bug）；endCall/mute 改走新 bot。已備份 `call.html.backup_20260707`。
  - ✅ **驗證**：preview 載入 call.html — 無 console error、`startGeminiVoicebot` 定義正確、點接聽同步流程正確（接通中→靜音/掛斷切換、AudioContext 建立無誤）、Edge Function 未部署時優雅顯示「⚠️ 無法連上 AI 客服」並 console.error 不崩潰。
  - ⚠️ **待部署真機驗證**（本機無麥克風＋Edge Function 未部署，無法端到端驗）：① Supabase 部署 `gemini-live-token` 並設 Secret `GEMINI_API_KEY`（沿用既有那把）；② push 到 main（GitHub Pages 更新 call.html）；③ 真手機開顧客連結接聽，跟 AI 用 Leda 聲音中文對話、測插話打斷。
  - ⚠️ **尚未做**：G2（讀 Supabase 資料庫回答＋建維修工單 function calling）、G3（AI 對話落地 conversations＋轉真人信令閉環）、G4（延遲優化）。下訂單因 CRM 無 orders/products 表列 G4 後另案。
  - 影響檔案：新增 `supabase/functions/gemini-live-token/index.ts`、`comm-voice/public/js/gemini-live-client.js`；改 `comm-voice/public/call.html`、`docs/voicebot路線圖.md`。

- 2026-07-07｜voicebot 全面盤點：bug 清單＋達標路線圖文件（純文件，未動程式碼）。（Claude）
  - 對 voicebot 前後端（`stt-worker/voicebot.py`＋`comm-voice/public/js/voicebot-client.js`＋`call.html`）做系統性盤點，產出 [`docs/voicebot路線圖.md`](docs/voicebot路線圖.md)：18 項 bug（P0×6／P1×7／P2×5，均附檔案:行號）＋ 4 階段達標路線圖（Phase 0 取得 TTS function-id 出聲 → Phase 1 修 P0 → Phase 2 AI 讀資料庫〔CRM 身分注入＋新建 kb_articles 知識庫輕量 RAG〕→ Phase 3 轉真人信令閉環＋AI 對話落地 conversations → Phase 4 streaming/barge-in 逼近 Gemini Live）。
  - 關鍵發現：①「AI 讀資料庫回答」目前 0% 實作（voicebot.py 無任何 Supabase 存取）；②call.html 50 秒計時器會在 AI 對話中誤彈留言表單（P0）；③轉真人無信令、客服端收不到通知（P0）；④AI 段對話不落地 CRM（P0）。
  - 後續 AI session 依該文件逐階段執行，每階段完成須真機端到端驗證＋補版本紀錄。
  - 影響檔案：新增 `docs/voicebot路線圖.md`、本檔。

- 2026-07-06｜即時 AI 語音客服 voicebot（取代 Gemini Live）修復一輪（尚未部署，等 TTS function-id）。（Claude）
  - 背景：`stt-worker/voicebot.py`＋`comm-voice/public/js/voicebot-client.js`＋`call.html` 是先前用其他工具做的即時語音客服（顧客接聽→AI 對話→講「找真人」就 `[TRANSFER]` 轉真人），一直**未提交、未部署**。本輪盤點並修主要 bug。
  - **前端 `voicebot-client.js` 重寫**：①WS 位址從寫死的 `ws://localhost:8000` 改為預設 `wss://waferlock-stt.onrender.com/ws/voicebot`（可 `window.VOICEBOT_URL` 覆寫）——正式站是 https 必須 wss、且要指向 Render 不是顧客自己電腦；②**半雙工防回音**：機器人講話時暫停送麥克風（否則麥克風收到喇叭放出的 TTS→ASR 又辨識機器人自己→無限自問自答），播完再等 350ms 才恢復收音；③getUserMedia 開 echoCancellation/noiseSuppression；④processor 串 gain=0 節點避免麥克風原音回饋；⑤TTS 音訊改用手動建 AudioBuffer（Riva 回裸 PCM，`decodeAudioData` 會失敗）。
  - **後端 `voicebot.py`**：①LLM 改用 `VOICEBOT_LLM_MODEL`（預設快的 `meta/llama-3.1-8b-instruct`，即時語音別用 397B 推理模型）、`max_tokens` 100→512、剝除 `<think>`；②TTS 用 `RIVA_TTS_FUNCTION_ID`＋`RIVA_TTS_VOICE`＋語言 `zh-CN`（TTS 沒有 zh-TW）＋LINEAR_PCM，回傳帶 `sampleRate`；③LLM/TTS 失敗顯性化（LLM 失敗改回「幫您轉接專員」而非丟錯字串）。
  - 新增 `stt-worker/check_tts.py`：本機測 TTS 能否發中文聲音（存 out_tts.wav）。
  - ⚠️ **尚未做**：①**取得中文 TTS 模型的 function-id**（build.nvidia.com Magpie TTS Multilingual 之類）——沒有它機器人不出聲；②本機用 check_tts 驗證發聲；③commit＋push 部署（部署後顧客接聽會變成 AI bot，需先確認可用）；④對話存進 CRM、轉真人後對面要有專員（尚未處理）。
  - ⚠️ TTS 限制：NVIDIA 只有 zh-CN（大陸普通話腔），無台灣國語腔。
  - 影響檔案：`comm-voice/public/js/voicebot-client.js`、`comm-voice/public/call.html`、`stt-worker/voicebot.py`、`stt-worker/main.py`（voicebot 掛載）、`stt-worker/check_tts.py`、`secret.txt.example`。

- 2026-07-07｜🔴 修復 conversations 訊息陣列並行寫入互相覆蓋（全庫盤點最高風險項）。（Claude）
  - 背景：`waferlock_crm.html`（3 處推播）、`waferlock_tech.html`（`logToConversation`）與四支 Edge Function（`line-webhook`/`meta-webhook`/`rtc-callback-request`/`rtc-recording`）全部是「SELECT 讀出 `msgs` → 應用層 append → 整包 UPDATE/UPSERT 寫回」。客服在 CRM 回覆的同時客戶恰好從 LINE 傳訊息（或 LINE/Meta 重送事件、師傅端與客戶同時觸發推播）時，後寫入者手上的 `msgs` 快照是舊的，整包寫回會把先寫入者剛存的那則訊息**無聲蓋掉**——沒有任何錯誤提示。
  - **新增 SQL RPC** `append_conversation_message`（[`sql/supabase_atomic_conv_append.sql`](sql/supabase_atomic_conv_append.sql)）：用 `INSERT ... ON CONFLICT DO UPDATE SET msgs = msgs || 新訊息` 收斂成單一原子陳述式，PostgreSQL 對同一列的並行寫入以列鎖序列化，後到的陳述式一定讀到前一個已 commit 的最新值，徹底消除「後寫蓋掉先寫」。
  - **四支 Edge Function**改用該 RPC 取代讀-改-寫：`line-webhook`（`handleMessage`）、`meta-webhook`（`handleMessaging`）、`rtc-callback-request`（同電話短時間二次逾時未接聽不再互相覆蓋）、`rtc-recording`（附加錄音訊息，`unread_delta:0` 沿用原「錄音不推高未讀數」行為，且維持原本「找不到既有對話就只存 Storage、不主動新建」的行為）。
  - **CRM 前端**新增共用 `appendConvMsg(convId,text)` helper，改寫 3 處推播（`submitRepair` 建單通知／`doRepairDispatch` 派工通知／`finishRepairClose` 結案通知）：不再把 `conversations` 塞進 `updateDB` 的整包 diff-upsert，改直接呼叫 RPC；本地 UI 不做樂觀更新，改靠既有 Realtime 訂閱（`postgres_changes` on `conversations`）自然補上最新對話內容（RPC 內部的 DB 寫入一樣會觸發 Realtime，跟直接 REST 寫入無差異）。
  - **師傅端**（`waferlock_tech.html`）`logToConversation` 同步改走 RPC。
  - ✅ 驗證：`waferlock_crm.html`／`waferlock_tech.html` 兩檔 Babel 編譯無 console 錯誤；已逐一核對三個 CRM 呼叫點移除 `pushMsg`/`nc`/`convs` 後無殘留引用。
  - ⚠️ **需要人工部署才會生效**：① 到 Supabase SQL Editor 執行一次 `sql/supabase_atomic_conv_append.sql`；② 重新部署四支 Edge Function（`supabase functions deploy line-webhook meta-webhook rtc-callback-request rtc-recording`，或 Dashboard 手動更新）。部署前，Edge Function 呼叫 RPC 會直接失敗（`function does not exist`）。
  - ⚠️ **本輪未觸及**（範圍外，留待後續）：`waferlock_LINE.html`（住戶端 LINE 模擬器＋0800 客服端）內多處 `msgs` 讀-改-寫尚未轉換（模擬器測試資料風險較低，但其中的 0800 客服端 agent 回覆路徑是真實產線，建議下一輪處理）；webhook 事件處理失敗仍回 200（LINE/Meta 平台不會重送，該筆事件永久遺失，屬盤點報告 #17）；`meta-webhook` 空 text 事件靜默丟棄（#18）。這幾項都跟本次修的「並行覆蓋」是不同機制的問題，需另案處理。
  - 影響檔案：`waferlock_crm.html`、`waferlock_tech.html`、`supabase/functions/line-webhook/index.ts`、`supabase/functions/meta-webhook/index.ts`、`supabase/functions/rtc-callback-request/index.ts`、`supabase/functions/rtc-recording/index.ts`、新增 `sql/supabase_atomic_conv_append.sql`。

- 2026-07-07｜全庫缺陷盤點後修復：同步靜默失敗顯性化 + 1000 列分頁上限 + 轉單時段回復。（Claude）
  - 背景：對整個 repo 做了一次系統性 bug 盤點（前端 CRM／LINE／師傅端／LIFF＋後端 Edge Functions），依嚴重度排序後從最高風險三項開始修。
  - **同步靜默失敗顯性化**：`syncToSupabase`/`loadFromSupabase` 舊版只包 `try/catch`，但 Supabase client 預設「失敗不丟例外」（`.upsert()`/`.select()` 失敗只在回傳值帶 `.error`），導致 RLS 擋下、型別錯誤等真正的寫入失敗完全被吞掉——畫面顯示操作成功、同步燈號仍亮著，重新整理後該筆異動卻消失。改為逐一檢查每個 Supabase 呼叫的 `.error`：`syncToSupabase` 回傳 `{ok,errors}`，`updateDB` 依此決定要不要把同步燈號轉離線＋跳 toast 告知哪些資料沒存上；初始載入失敗不再靜默回傳空資料庫（避免客服誤以為系統正常、重複建立客編），改顯示錯誤畫面＋「重新連線」按鈕。
  - **1000 列分頁上限**：`loadFromSupabase` 原本十張表都用 `select('*')`，PostgREST 預設單次最多回 1000 筆；任一張表資料量超過門檻後，多出的資料會在使用者眼中「無聲消失」（查不到客戶、看不到舊工單）。新增 `selectAllRows(table)`，用 `.range()` 分頁抓到底再合併，一勞永逸解決成長後的資料量問題。
  - **轉單時段回復**：`doRepairDispatch`/`doInstallSchedule` 轉單改派時，會先 `releaseSlots` 釋放舊時段、再 `bookSlots` 佔新時段；若新時段恰好衝突失敗，舊時段已經被釋放卻沒有復原，導致工單顯示「已派工＋原師傅」，工務時刻表卻是空格的中間態。改為呼叫前先記住舊時段（師傅/日期/起始時/格數），衝突失敗時原樣復原，並在錯誤訊息中告知「原時段已還原」。
  - 影響檔案：`waferlock_crm.html`（`loadFromSupabase`/`syncToSupabase`/`updateDB`/初始載入 `useEffect`/`doRepairDispatch`/`doInstallSchedule`）。
  - ✅ 驗證：Babel 編譯無 console 錯誤；三項修改皆已核對呼叫端與依賴陣列正確銜接。
  - ⚠️ 同一輪盤點還有更高風險的「`conversations` 訊息陣列並行寫入互相覆蓋」問題（涉及 4 支 Edge Function + CRM 前端 + 新 SQL RPC），規模較大，另案處理中。

- 2026-07-06｜NVIDIA STT 雲端全通（逐字稿+摘要）+ 錄音掛回正確客戶。（Claude＋使用者）
  - 對已部署的 Render 服務實測真人中文錄音：`transcript`=「你好我想要買電子鎖」、`summary`=「客戶欲購買電子鎖，需安排專人介紹產品型號與安裝事宜。」（Qwen `qwen/qwen3.5-397b-a17b`）。整條 NVIDIA 管線（Riva zh-TW ASR + Qwen 摘要）雲端跑通。
  - 排錯：①`main.py` 漏 `import wave`（NameError→逐字稿空）；②LLM 模型 ID 要含廠商前綴 `qwen/`（少了回 404）。皆已修，並在 `/stt` 回應加 `asr_err`/`llm_err` 除錯欄位。
  - **錄音掛回正確客戶**：`attach_to_conversation` 改成 **upsert**（電話對話不存在就建立，帶 `platform:'phone'`＋`wf_id`）；`/stt` 新增接收 `wfId`。前端 `agent-call.html` 轉發 `wfId`；`waferlock_LINE.html` 的 `voiceUrls` 把 `conv` 改為電話對話 id（`phone_<純數字>`，與 `submitACW` 一致）＋帶 `wfId`。這樣通話錄音會落到同一通電話對話、掛到正確客戶。
  - **顯示補齊**：`waferlock_crm.html` 客戶歷史紀錄 + `waferlock_LINE.html` `CustomerInfoPanel` 的通話來源，除了 `acw` 也納入 `recording` 類型（顯示「🎙️ 通話錄音｜摘要」＋「含逐字稿」）。
  - ✅ 驗證：兩 HTML preview 編譯無誤、console 零錯誤；worker `py_compile` 過。⚠️ **最終端到端**（真機打中文電話→掛斷→客戶 CRM 出現帶逐字稿的錄音）待 Render/Pages 重部署後由使用者實撥確認。
  - 影響檔案：`stt-worker/main.py`、`comm-voice/public/agent-call.html`、`waferlock_LINE.html`、`waferlock_crm.html`。

- 2026-07-06｜NVIDIA STT 服務上線 + 接回前端（實測中文辨識成功）。（Claude＋使用者）
  - `stt-worker` 已部署到 Render：`https://waferlock-stt.onrender.com`（Free 方案，閒置會休眠、首次請求約 +50 秒喚醒，對錄音處理無妨）。`/healthz` 回 `riva_ready:true`（NVIDIA 金鑰 + function-id + zh-TW 皆正確吃到）。
  - 本機用真人中文錄音（電子鎖.m4a → ffmpeg 轉 16k 單聲道 wav）實測 `check.py`：辨識出「你好我想要買電子鎖」。過程解掉三個坑——①zh-tw 模型只支援 **streaming**（offline 回 Unavailable model）；②要補 **sample_rate_hertz**（否則 sample_rate=0）；③冷門術語「電子鎖」會被聽成「電子書」，加 **word boosting**（電子鎖/維夫拉克/門鎖…，中文每字空格）後正確。這三項都已寫進 `stt-worker/main.py`。
  - `comm-voice/public/agent-call.html`：`STT_WORKER_URL` 填上 Render 網址 → 錄音改送 NVIDIA 服務（留空則回退 Gemini）。
  - Supabase 已建私有 bucket `call-recordings`。
  - ⚠️ **尚未做的最終驗證**：真機打一通中文電話 → 掛斷 → 客戶 CRM 的通話紀錄該筆帶「含逐字稿」、`recording.transcript` 有中文逐字稿。需 Pages 部署完再撥一通確認端到端。
  - 影響檔案：`comm-voice/public/agent-call.html`（接上服務網址）；`stt-worker/{main.py,check.py}`（streaming/sample_rate/加強詞修正，前面幾筆已記）。

- 2026-07-06｜STT 改用 NVIDIA：新增 `stt-worker/` Python 服務（Riva zh-TW ASR + NVIDIA LLM 摘要）。（Claude）
  - 決策：使用者有 NVIDIA build.nvidia.com 帳號，選用 NVIDIA 取代 Gemini。查證後確認 NVIDIA ASR（parakeet-ctc-0.6b-**zh-tw**，台灣繁中）走 **gRPC**（`grpc.nvcf.nvidia.com`），Supabase Edge Function(Deno) 打不了 gRPC，故另建一支 Python 服務承接。
  - 新增 `stt-worker/`（FastAPI）：`/stt` 收前端 webm+wav → 存 Supabase Storage → `nvidia-riva-client` gRPC 轉逐字稿(zh-TW) → NVIDIA LLM（`integrate.api.nvidia.com`，OpenAI 相容）產一句摘要 → 寫回 conversations 一則「🎙️ 通話錄音」訊息（含 transcript/summary）。金鑰/function-id/模型全走環境變數，不硬編碼。
  - `comm-voice/public/agent-call.html`：錄音上傳改成可切換——設了 `STT_WORKER_URL`（NVIDIA 服務）就走它，留空則回退 Supabase `rtc-recording`（Gemini 路線）。兩條路並存，改一行切換。
  - ✅ 驗證：`main.py` 通過 `py_compile` 語法檢查。⚠️ 實跑需使用者部署 Render + 設環境變數（尤其到 build.nvidia.com 的 parakeet zh-tw 模型頁「Try API」抓 `function-id`）+ 建 `call-recordings` bucket，且真機講一通中文電話才驗得到逐字稿。
  - 影響檔案：新增 `stt-worker/{main.py,requirements.txt,README.md}`；改 `comm-voice/public/agent-call.html`。
  - 備註：先前的 `rtc-recording`（Gemini）保留為回退方案，不刪。

- 2026-07-06｜0800 語音按鈕放大 + 錄音 STT 逐字稿。（Claude）
  - **語音按鈕放大／修可見度**（`waferlock_LINE.html`）：0800 通話卡片內「開客服端語音／複製手機連結」原本沿用 `cc-ctlbtn`（白字＋半透明白底，是給頂部深色列用的），放在白色卡片上變成白字白底幾乎看不見、只剩 emoji。新增專屬 `.cc-voicebtn`（primary=紫色實心大按鈕、ghost=白底描邊），字體 11→13.5px、加大內距，響鈴卡與通話中卡都換上。
  - **錄音 STT（`rtc-recording`）**：把原本「只產一句摘要」的 `summarize` 擴充為 `analyze`，用 Gemini 2.0 Flash 的 JSON 輸出（responseSchema）同時回 `transcript`（逐字稿，分辨客服／顧客、逐句換行）與 `summary`（一句摘要）。錄音訊息的 `recording` 欄位多存 `transcript`，摘要行標「含逐字稿」。需設 `GEMINI_API_KEY` 才會轉寫，沒設就只存錄音（不擋主流程）。
  - ✅ 驗證：LINE.html preview 編譯無誤、console 零錯誤、`.cc-voicebtn.primary` 樣式生效（紫底）。rtc-recording 本機無 deno 靠審查。
  - ⚠️ STT 要等 `rtc-recording` 部署 + 建 bucket + 設 `GEMINI_API_KEY` 才會實際產出；逐字稿目前只落在 conversations 訊息的 `recording.transcript`，前端尚未做「展開看逐字稿」的 UI（下一步可加）。
  - 影響檔案：`waferlock_LINE.html`、`supabase/functions/rtc-recording/index.ts`。

- 2026-07-06｜0800 來電通話紀錄同步進客戶 CRM 資料（凡走過必留下痕跡）。（Claude）
  - 需求：使用者要求「誰處理過、處理了什麼都要留在客戶資料內」，0800 完成話後處理（ACW）時，通話摘要要像維修/安裝/客訴一樣出現在客戶的紀錄時間軸。
  - **關鍵發現：資料早就同步了，缺的只是顯示**。`submitACW` 完成時已把通話寫進 `conversations`（`platform:'phone'`、帶 `wfId`、每則 acw 含 `by`/`acw:{agent,cat1,cat2,cat3,durationSec}`），且 `waferlock_crm.html` 本來就有載入 conversations。所以**不建新表、不跑 SQL、不改寫入邏輯**，只在顯示層多撈「通話」這一種來源即可。
  - `waferlock_crm.html` 客戶詳情「歷史紀錄」：新增 `curCalls`（撈該客戶 wfId 的 phone 對話中每則 `type==='acw'` 訊息），併入時間軸、加綠色「通話」標籤、計入 `historyCount`；通話列因無獨立案件頁，「開啟此案」按鈕改顯示「👤 客服名」。
  - `waferlock_LINE.html` 的 `CustomerInfoPanel`（AgentPage 與 0800 桌面共用）：「📁 案件紀錄」縮影同步加入「來電」類型（分類三階＋客服名），該類型不顯示會壞的「查看詳情」連結、改顯示「📞 通話紀錄」。
  - ✅ 驗證：兩檔 preview 載入，Babel 編譯無誤、console 零錯誤、root 正常渲染。⚠️ 因需正式帳號登入＋有通話資料的客戶才看得到實際列，登入後的實際顯示未點測（顯性告知）。
  - 影響檔案：`waferlock_crm.html`、`waferlock_LINE.html`（皆顯示層，無資料結構/寫入變更）。

- 2026-07-06｜comm-voice 階段3 補齊：新增錄音落地 Edge Function `rtc-recording`。（Claude）
  - 補上階段3 缺的另一半（待回電 `rtc-callback-request` 由 Codex 已做）。前端 `agent-call.html` 掛斷後送 multipart（webm + 選配 16k wav + conv/name/durationSec），此 function：① webm 上傳私有 bucket `call-recordings`（路徑 `<conv>/<ts>.webm`）；② 若 `GEMINI_API_KEY` 有設，用 wav 丟 Gemini 產一句話通話摘要（沒設或失敗都回空字串，不擋主流程）；③ 若 `conv` 對得上既有 conversation 就附一則「🎙️ 通話錄音」訊息（含時長/摘要/storage 路徑），對不上就只存 Storage。
  - ⚠️ **部署前置**：需先在 Supabase Storage 建一個 bucket `call-recordings`（建議私有）；`supabase functions deploy rtc-recording`；環境變數 `SUPABASE_SERVICE_ROLE_KEY`（必要）、`GEMINI_API_KEY`（選配）。未部署前，前端送錄音會 404（agent-call.html 的 onRec 有 try/catch，不影響通話）。
  - ⚠️ 本機無 deno 無法跑測，語法靠審查（與專案其他 edge function 一致）。
  - 影響檔案：新增 `supabase/functions/rtc-recording/index.ts`。

- 2026-07-06｜comm-voice 階段3（待回電）+ 階段4（接進 0800 桌面）+ 首頁案件池優化。（Codex 實作，Claude 驗證＋補紀錄）
  - **背景**：這批由 Codex CLI 於本日早上實作，先前未提交、未補版本紀錄。Claude 事後盤點、preview 驗證兩個大 HTML 編譯無誤後補紀錄並提交。
  - **階段4 — 真人語音接進 0800 值機桌面**（`waferlock_LINE.html` 的 `EightZeroZeroPage`）：
    - 新增 `makeVoiceRoom()`（產 `call_<base36>` 房號，ASCII，符合 Realtime topic 限制）、`voiceUrls()`（以 LINE 頁位置為基底組 agent-call/call 網址）、`startVoiceCall()`（開客服端新分頁）、`copyCustomerVoiceLink()`（複製手機接聽連結到剪貼簿）。
    - 響鈴卡與通話中卡各加「🎙️ 開客服端」「🔗 複製手機接聽連結」按鈕，並顯示房號。取代原本純模擬來電——現在能真的把顧客接聽連結發出去。
  - **階段3 一半 — 新增 Edge Function `supabase/functions/rtc-callback-request/index.ts`**：顧客端 50 秒無人接聽 → 待回電 upsert 進 `conversations`（`platform:'phone'`、`need_case:true`、`agent_takeover:true`），時間戳用 24 小時制 `tsNow()`（沿用先前修過的時間戳教訓）。前端 `call.html` 早已指向此 function 位址，兩邊對得上。⚠️ **待部署**：跟 rtc-config 一樣要 `supabase functions deploy rtc-callback-request`（需 `SUPABASE_SERVICE_ROLE_KEY`）才會生效。
  - **首頁案件池優化**（`waferlock_crm.html` 的 `HomePage`，與語音無關，屬「未來可改善」清單項目）：客訴單列表新增日期區間篩選（`dateFrom`/`dateTo`）、排序（最新／最舊在前）、分頁（每頁 30 筆）、每張卡片「複製案件連結」按鈕。
  - **新增 `AGENTS.md`**：Codex 版專案協作守則（對應 `CLAUDE.md`），同步「先讀版本紀錄、外科手術式修改、顯性失敗」等既有契約。
  - ✅ **驗證**：`waferlock_LINE.html`／`waferlock_crm.html` 以 preview 載入，Babel Standalone **編譯無誤、console 零錯誤、root 正常渲染**（codex 改大 HTML 的主要風險＝JSX 語法錯誤，已排除）。
  - ⚠️ **尚未做**：0800 語音按鈕與首頁篩選的**登入後實際點擊**未驗證（需正式帳號）；`rtc-callback-request` 未部署；階段3 的**錄音** `rtc-recording` function 尚未撰寫。
  - 影響檔案：`waferlock_LINE.html`、`waferlock_crm.html`、新增 `supabase/functions/rtc-callback-request/index.ts`、新增 `AGENTS.md`。

- 2026-07-04｜comm-voice 改走「B 路」：信令改用 Supabase Realtime、TURN 憑證改用 Edge Function（可放 GitHub Pages，不用養常駐伺服器）。（Claude）
  - 決策：使用者選 B 路——後端全掛既有 Supabase，前端三支靜態檔放 GitHub Pages，不另養 Render。
  - **新增 Edge Function `supabase/functions/rtc-config/index.ts`**：Deno 版產 ICE servers（STUN + Cloudflare TURN 時效憑證），金鑰 `CF_TURN_KEY_ID`/`CF_TURN_API_TOKEN` 走 Secrets。
  - **`comm-voice/public/js/rtc-client.js` 信令傳輸層改寫**：從自建 WebSocket（`/api/rtc/signal`）改成 Supabase Realtime broadcast（頻道 `rtc-<room>`）。WebRTC 核心（makePc/協商/錄音）完全不動。
  - **兩支通話頁**：載入 `@supabase/supabase-js@2` CDN、`/api/rtc/config` 改指 Edge Function `rtc-config`、傳 `supabaseUrl`/`supabaseKey` 給 WLCall；錄音/回電位址改指 `rtc-recording`/`rtc-callback-request`（階段3 才建）。
  - **信令設計幾經迭代才穩**（都對真實 Supabase Realtime 實測）：
    - 初版用 presence 判對端進退→踩到「對端已在房內時 join 事件不補發」→改成每次 sync 用 presenceState 重算。
    - 再踩到「剛訂閱就送 broadcast 會被 supabase-js 退回 REST 傳送、對端收不到」的競態——presence 觸發的搶發 offer 會漏接。
    - 最終定案：**丟掉 presence，改「SUBSCRIBED 後週期性送 hello 探索 + agent 收 hello 發 offer 並重送直到 answer」**，所有送出都在 SUBSCRIBED 之後、且重送容忍首則遺失。掛斷走 `bye`，非優雅斷線靠 WebRTC connectionState。
    - ⚠️ **關鍵教訓**：Supabase Realtime 頻道 topic（room 名）**必須 ASCII**。測試 room 名一度含中文「先」字，導致 WebSocket join 失敗、全部 send 退回 REST 而收不到，卡了很久才定位。實際 app 的 room 是 `call_xxx` 這類 ASCII，不受影響；已在 rtc-client.js 與 README 註明。
  - ✅ **驗證**：`comm-voice/test-signal-supabase.js` 對**真實 Supabase Realtime** 跑「agent 先進房」與「customer 先進房（agent 後進，原設計會 hang 的關鍵情境）」兩種順序，各 5 項（offer/收 offer/answer/ice/bye）**全部通過、0 個 REST fallback 警告**。preview 驗證兩頁渲染正常：supabase CDN 載入、WLCall 定義、config/supabaseUrl 到位、console 零錯誤。
  - ⚠️ **尚未完成**：真機跨網路對講實測（需部署 + 實體麥克風）；階段3 兩支 Edge Function（rtc-recording/rtc-callback-request）；階段4 接 0800 桌面。
  - 影響檔案：新增 `supabase/functions/rtc-config/index.ts`、`comm-voice/test-signal-supabase.js`；改 `comm-voice/public/js/rtc-client.js`、`comm-voice/public/agent-call.html`、`comm-voice/public/call.html`、`comm-voice/README.md`；`.claude/launch.json`（comm-voice 埠 3000→3100，3000 被 OS 保留）。

- 2026-07-04｜comm-voice 階段2（程式面）：TURN 中繼串接完成並實測生效。（Claude）
  - 使用者申請 Cloudflare Realtime（前身 Calls）TURN，取得 Turn Token ID + API Token，自行設環境變數驗證（金鑰不經手第三方）。
  - 修兩個 bug：① Cloudflare 端點舊寫成 `/credentials/generate`，改為現行 `/credentials/generate-ice-servers`；② 該端點回的 `iceServers` 是**陣列**（舊端點是單一物件），原本 `unshift(cf)` 把整包當一個元素塞入導致**巢狀陣列**（不合法、WebRTC 會忽略），改為 `unshift(...cf)` 攤平。
  - 過程排錯：一度 HTTP 404 `cannot find specified key`，原因是 Turn Token ID 在後台是截斷顯示、使用者反白複製只拿到一截；改用複製按鈕取完整 ID 後解決。已在 `cloudflareTurn()` 加除錯 log（印 HTTP 狀態/keyId 長度/回應摘要，不含完整密鑰）方便日後診斷。
  - ✅ 驗證：`/api/rtc/config` 實測回傳平陣列，含 Cloudflare STUN、Cloudflare TURN（帶時效 username/credential）、Google STUN 保底，格式合法。
  - ⚠️ **尚未完成**：真機跨網路實測（手機行動網路 vs 電腦 Wi-Fi 各開一端、同 room 對講）——這步需實體麥克風 + 對外 https 網址（ngrok 或部署 Render），無頭環境測不了。TURN「有沒有真的在中繼」要靠這步最終確認。
  - 影響檔案：`comm-voice/server.js`。

- 2026-07-04｜逆向工程還原「真人語音通話」功能（新增 `comm-voice/` 子專案，階段0+1）。（Claude）
  - 背景：使用者提供一個運作中的外部網站 `https://waferlock-comm-cloud.onrender.com`（廠商做的整套「維夫拉克溝通雲」SaaS），要求逆向還原其中的「真人語音通話」功能。前提：對原站原始碼/部署完全無存取權（黑箱）、只要語音通話這一塊、前後端都要重建。計畫書見 `.claude/plans/`（已批准）。
  - **逆向結果**：這是瀏覽器原生 WebRTC P2P 語音——客服端 `agent-call.html`（開啟自動撥號、role=agent 發 offer）、顧客端 `call.html`（點接聽才連、role=customer 回 answer、50秒無人接→留言回電表單）、共用 `rtc-client.js`（信令走 WebSocket `/api/rtc/signal`，訊息型別 join/offer/answer/ice/bye + joined/peer-join/peer-leave；ICE trickle；接通後混錄自己+對方成 webm）。三支前端檔皆為公開可下載，已逐字還原。後端四支端點（config/signal/recording/callback-request）為黑箱，只能靠呼叫合約反推重寫。
  - **階段0（資產落地）**：三支前端檔 + logo 下載到 `comm-voice/public/`；`/api/rtc/config` 活樣本存 `comm-voice/ice-config-sample.json`。
  - **階段1（信令+通話跑通）**：新增 `comm-voice/server.js`（Express + ws，~140 行）自建後端：
    - `GET /api/rtc/config` — 回 ICE servers（預設 Google STUN；設環境變數後含 Cloudflare Calls TURN 或靜態 TURN，金鑰走環境變數不硬編碼）。
    - `WS /api/rtc/signal` — 房間信令中繼（in-memory `Map<room,Set<ws>>`），完整實作 rtc-client.js 期望的協定。
    - `POST /api/rtc/recording`、`POST /api/rtc/callback-request` — 階段1 先收下不報錯（回 `stored:false`），階段3 才落 Supabase/Gemini。
  - ✅ **驗證**：① `comm-voice/test-signal.js` 模擬 agent+customer 兩端跑信令，7 項檢查全過（peerPresent 邏輯、peer-join、offer/answer/ice 轉發、peer-leave）。② preview 起服務，`agent-call.html`／`call.html` 皆正確渲染（URL 參數解析對、logo 正常、console 零錯誤）；客服端自動撥號流程有啟動並在無麥克風的無頭環境正確停在「需要麥克風權限」錯誤狀態。③ 四支端點 curl 實測皆正常回應。
  - ⚠️ **尚未完成**：階段2（TURN 供應商申請 + 跨網路實測——**未設 TURN 前只有同區網能通，不能宣稱跨網路可用**）、階段3（錄音存 Supabase + Gemini 摘要 + 待回電落 DB）、階段4（接進 `waferlock_LINE.html` 0800 值機桌面取代模擬來電）。
  - ⚠️ **真人語音的實際音訊未在本機端到端驗證**：無頭環境沒有實體麥克風，`getUserMedia` 必然失敗；WebRTC P2P 音訊是瀏覽器原生、且 `rtc-client.js` 是正式站逐字取得的已驗證程式碼，我能驗證的是自寫的信令伺服器（已全過）。真正兩台裝置對講需在真機測。
  - 影響檔案：新增 `comm-voice/`（獨立 Node 子專案，非 GAS）；`.gitignore`（加 node_modules / LINE DATA）；`.claude/launch.json`（加 comm-voice 預覽設定）。

- 2026-07-03｜正式環境驗證 SQL 遷移 + wf_id 撞號修復（`sql/supabase_date_format_migration.sql`、production 資料）。（Claude）
  - 背景：使用者告知兩支 SQL 已在 Supabase SQL Editor 執行、兩支 Edge Function 也已重新部署，請我驗證。用 REST API 直接查 production 資料抽查結果：
  - 🔴 **發現：日期遷移腳本 regex 寫錯，執行了等於沒執行**。原腳本比對 `^\d{1,2}/\d{1,2}/\d{4}$`（M/D/YYYY），但實際壞資料是 `toLocaleDateString('zh-TW')` 產生的 **YYYY/M/D**（年在前，如 `"2026/7/2"`），regex 永遠比不中，UPDATE 命中 0 筆，資料完全沒轉換——這是我上一輪寫腳本時的疏失，不是使用者操作問題。已修正 `sql/supabase_date_format_migration.sql` 的 regex 與 `to_date()` 格式字串（改用 `'YYYY/MM/DD'`），並對 production 目前殘留的 13 筆資料（`customers.reg_date`×2、`customers.install_date`×1、`repairs.date`×6、`repairs.completed_date`×2、`complaints.date`×2、`complaints.closed_date`×1）用逐筆 PATCH 手動轉換完畢，全表複查已無殘留 YYYY/M/D 格式。
    - ⚠️ 過程插曲：第一次嘗試用 `python3` 做日期轉換時，該指令在此環境不可用而靜默失敗，導致誤將上述欄位 PATCH 成空字串，立即發現後改用 `awk` 重新計算並二次 PATCH 修正回正確 ISO 值，已重新查表確認全部非空、格式正確。
  - 🔴 **發現：`wf_id` 撞號風險**。`wf_counters` 是全新建表（`seq` 從 0 起算），但資料庫裡已有一筆舊種子客戶 `wf_id = "WF-2026-00001"`（程心妤，`reg_date: 2024/03/15`，`repair_ids` 指向的 `R-001` 實際上 `wf_id` 是別的客戶，本身資料就不一致，判斷是早期測試資料）。若不處理，下一個真實客戶第一次呼叫 `next_wf_id()` 就會拿到跟這筆舊資料相同的客編。經使用者確認「資料都可刪除」，已刪除該筆舊客戶（`repairs`/`installs`/`complaints` 均無其他關聯，刪除安全），並將 `wf_counters` 的 `WF2026.seq` 重設為 0。
  - ✅ **Edge Function 部署確認**：`meta-webhook`／`line-webhook` 皆回應非 404（分別是 403 驗證失敗、401 bad signature），確認兩支都已部署上線且驗證邏輯生效。
  - ⚠️ **待確認**：全表掃描 `conversations.msgs`，目前 0 筆訊息帶有 `ts` 欄位（含近期真實 FB 訊息）。無法判斷是「兩支函式部署的是舊版程式碼（缺 `fullTs()`）」還是「所有現有訊息都剛好在部署之前送達」。建議使用者實際發一則新的 LINE/FB/IG 測試訊息，之後我可以再查一次 `conversations` 確認新訊息是否帶 `ts`。
  - 影響檔案／資料：`sql/supabase_date_format_migration.sql`（腳本修正）；production `customers`/`repairs`/`complaints`/`wf_counters` 表資料（直接修正，非透過腳本批次跑，因目前殘留筆數少，用逐筆 PATCH 更可控）。

- 2026-07-03｜修正 `ts` 欄位格式錯誤（`waferlock_LINE.html`）。（Claude）
  - 背景：上一輪新增的訊息 `ts` 欄位（供客服績效報表算回覆時效）寫法是 `today()+' '+now()`，但 `now()` 是 zh-TW 12小時制（含「上午/下午」文字），部署後在正式環境實測，實際寫入的 `ts` 長這樣：`"2026-07-03 下午02:32"`——這種格式 `new Date()` 解析不了，會讓 `CsPerfReportPage` 的回覆時效計算把這些訊息全部靜默判定為無效資料而跳過，功能等於是壞的但不會報錯，使用者實測時才發現。
  - **修法**：新增 `tsNow()` 函式，直接用 `Date.getHours()`/`getMinutes()` 手動組出 24 小時制字串（如 `"2026-07-03 14:34"`），不經過 `toLocaleTimeString` 的地區化格式。`sendMsg()`、`submitACW()`、`syncMsgToAgent()` 三處的 `ts` 欄位改呼叫 `tsNow()`。既有的 `now()`（聊天泡泡顯示用）完全不動。
  - 已檢查兩支 Edge Function（`line-webhook`/`meta-webhook`）的 `fullTs()` 寫法本來就是手動組 24 小時制，沒有這個問題，不用改。
  - ✅ 驗證：本機 `tsNow()` 直接呼叫確認輸出 `"2026-07-03 14:34"`，`new Date(ts)` 解析成功且值正確。
  - ⚠️ 教訓：新增可被程式解析的時間戳欄位時，不能直接複用「給人看」的 `now()`——這類地區化格式函式的字串不保證能被 `new Date()` 讀回，要嘛用手動組字串，要嘛用 `toISOString()`。
  - 影響檔案：`waferlock_LINE.html`。

- 2026-07-03｜計畫書2 §3.3/§3.4：補客服績效兩個資料缺口 + 全報表 CSV 匯出。（Claude）
  - 背景：使用者要求同時做「1. 補資料缺口把客服績效報表做完整」與「2. CSV 匯出」。

  **① 補資料缺口 1——訊息回覆者/完整時間戳**
  - `waferlock_LINE.html` 原本完全沒有登入/角色機制，不知道「誰在回訊息」。新增輕量客服身分識別（NavBar 文字輸入框「👤 你的客服/座席名稱」，存 localStorage，**非帳號登入驗證**，僅供歸屬統計用）。
  - `sendMsg()`（AgentPage 真人回覆）、`submitACW()`（0800話後處理）的訊息物件補上 `by`（回覆者）與 `ts`（`YYYY-MM-DD HH:MM` 完整時間戳，取自已修正的 `today()`+`now()`）；`syncMsgToAgent()`（客戶端訊息模擬）的 user 訊息也補 `ts`，兩端才能互相計算時間差。既有的 `time`（純 HH:MM，聊天泡泡顯示用）完全不動，`ts` 是新增的平行欄位，不影響任何既有畫面。
  - **兩個 Supabase Edge Function 也需要同步補**（`supabase/functions/line-webhook/index.ts`、`meta-webhook/index.ts`）：真實客戶從 LINE/FB/IG 傳來的訊息是走這兩支雲端函式落地，不是走 HTML 前端，所以也补上 `fullTs()` 產生的 `ts` 欄位。
  - ⚠️ **部署前置條件（我無法自己做）**：這兩支 Edge Function 改完程式碼後**必須由你手動透過 Supabase CLI/後台重新部署**才會生效，前端程式碼修改無法讓雲端函式自動更新。部署前，真實客戶訊息不會有 `ts` 欄位，回覆時效報表會少算這些筆數（不會報錯，只是暫時被忽略，等部署後新訊息才開始計入）。

  **② 補資料缺口 2——0800 ACW 結構化**
  - 好消息：`submitACW()` 內部的 `duration`（秒數）、`acwCat1/2/3`（三階分類）其實本來就是分開的 state，只是寫入資料庫時全部被字串化塞進單一 `text` 摘要。改為在訊息物件多存一個 `acw:{agent,cat1,cat2,cat3,durationSec}` 結構化欄位，`text` 摘要原樣保留（畫面顯示不受影響），純粹是多存一份給報表用的乾淨資料。

  **③ 客服績效報表接上新資料**
  - `CsPerfReportPage` 新增兩個區塊：「💬 訊息回覆時效」（本月 user→下一則 agent 首次回覆的時間差，整體平均＋依人分組）、「📞 0800 座席統計」（座席通話量/平均時長、ACW分類分佈）。計算邏輯只認有 `ts` 欄位的新資料，舊資料（無 `ts`）自動略過，不會誤算。頁面補一段說明「自 2026-07-03 起才有完整資料，數字會隨新資料累積而變準確」，取代原本「尚未納入」的警示。
  - ✅ 驗證：本機載入 Babel 編譯無誤、console 無錯誤；因為欄位剛上線，正式環境目前無新格式資料可驗證計算結果，畫面正確顯示「本月無可計算資料」的預期空狀態。

  **④ 資料匯出（計畫書2 §3.4）**
  - 新增共用工具 `exportCsv(filename,headers,rows)`（`waferlock_crm.html`）：CSV 帶 UTF-8 BOM（Excel 開繁中不亂碼）、`\r\n` 換行、逗號/引號正確跳脫，用 Blob+`<a download>` 觸發瀏覽器下載，零後端改動。共用元件 `ExportButtons`（彙總/明細兩顆按鈕）。
  - 三張報表（師傅報表、品質與派工報表、客服績效報表）都接上：**彙總 CSV**（畫面上的統計數字）＋**明細 CSV**（逐筆工單/客訴清單）。明細含姓名/電話的（師傅報表、客服績效報表），非 admin 角色強制套用既有的 `maskName`/`maskPhone` 遮罩（沿用案件列表已在用的同一套遮罩函式，不是新規則）；品質與派工報表明細不含客戶個資，不需遮罩。新增 `maskAddress` 供未來報表需要時使用。
  - ✅ 驗證：正式環境 admin 帳號實際點擊三張報表的兩顆匯出按鈕（用攔截 Blob 內容的方式檢查，未真的落地下載檔案到硬碟），確認 CSV 內容、表頭、BOM、資料正確；admin 身分看到未遮罩的真實姓名/電話（符合設計，未截圖／未外流）。
  - 未測：非 admin 角色的遮罩實際效果（需要用非 admin 帳號登入才能驗證，本輪用 admin 測試）。
  - 影響檔案：`waferlock_crm.html`、`waferlock_LINE.html`、`supabase/functions/line-webhook/index.ts`、`supabase/functions/meta-webhook/index.ts`。

- 2026-07-03｜計畫書2 §3.3：新增「客服績效報表」頁面（客訴類指標）。（Claude）
  - 背景：計畫書2 §3.3 把客服績效報表拆成「資料已就緒可先做」（客訴類指標）與「需先補資料缺口」（訊息回覆時效、0800座席統計）兩塊，使用者確認先做前者。
  - **新增 `CsPerfReportPage`**（`waferlock_crm.html`，NavBar「🎧 客服績效報表」）：
    - 建單量排行：本月建立的維修單＋客訴單，依 `createdBy` 分組統計（維修/客訴分開列）
    - 客訴結案率＋平均結案天數：以 `complaints.date`→`closedDate` 計算
    - 追蹤逾期清單：即時快照（不受月份篩選）——未結案且最新一筆 `type:'schedule'` 追蹤紀錄的 `followUpDate` 已過期
    - 頁面底部固定顯示資料缺口警示，說明訊息回覆時效與0800座席統計為何還沒做（對應計畫書2 §3.3 缺口1/2）
  - 月份篩選與日期解析邏輯沿用 `WorkerReportPage`/`QcReportPage` 已驗證過的 `inMonth()` 寫法。
  - ✅ **端到端驗證（正式 Supabase，admin 帳號唯讀瀏覽，未寫入任何資料）**：登入後切換到新頁面，本月客訴建單數／建單量排行（系統管理員4件、LINE自助報修1件）／追蹤逾期清單皆用真實資料正確渲染，console 無錯誤。
  - 未動：訊息回覆時效、0800座席統計（待 `conversations.msgs` 回覆者欄位與0800 ACW結構化補齊）、資料匯出（CSV）。
  - 影響檔案：`waferlock_crm.html`。

- 2026-07-03｜計畫書2 §3 報表章節改版：補客服績效、故障問題報表與資料匯出規格。（Claude）
  - 背景：使用者指出報表不能只有師傅報表，需納入客服績效報表、故障問題報表，且所有報表要能匯出資料。原 §3.2 只是 6 列總表，改版為各自成節的規格。
  - **§3.2 故障問題報表**：註明第一版「品質與派工報表」頁已於本日上線；擴張規格列出故障細項下鑽、處理過程分佈、通路/區域/保內外切片（資料皆已在收 ✅）。
  - **§3.3 客服績效報表（新）**：定義 5 項指標（建單量/客訴結案率/追蹤逾期/0800 通話統計/訊息回覆時效），並依實際程式碼探查**標記兩個資料缺口作為廠商前置修補要求**——① `conversations.msgs[]` 無回覆者欄位且 `time` 只有 HH:MM 無日期，回覆時效算不出來；② 0800 ACW 只把摘要塞進 text 字串，座席/分類/通話時長未結構化。客訴類指標（created_by/timeline 有完整歸屬）可先做。
  - **§3.4 資料匯出（新，橫切所有報表）**：彙總＋明細兩種 CSV、UTF-8 BOM（Excel 繁中不亂碼）、檔名規則、個資遮罩規則（非 admin 強制遮罩，與 deidentify.py 同源）；現階段前端產 CSV、重建後改後端產檔的過渡路徑。全系統目前零匯出功能，此為新增需求。
  - **§3.5/§3.6**：營收/對帳/B2B 請款（依賴外部條件）獨立成節；技術建議順移改號並補「匯出屬第2期」。
  - **§5 路線圖第2期**與 **§6 第2期驗收**同步擴充（+4 條：CSV 匯出無亂碼、非 admin 遮罩、客服績效前置欄位補完、三指標與人工抽查一致）。
  - 純文件改版，未動任何程式碼。
  - 影響檔案：`docs/計畫書2_擴張藍圖_金流報表0800.md`。

- 2026-07-03｜計畫書2（擴張藍圖）§3 報表：新增「品質與派工報表」頁面。（Claude）
  - 背景：[`docs/計畫書2_擴張藍圖_金流報表0800.md`](docs/計畫書2_擴張藍圖_金流報表0800.md) §3.2 標記 QC 品質報表、派工使用率「資料就緒度 ✅」——QC三階段分類、bookings 排班資料都已在收，只差呈現，不像營收儀表板/對帳報表要等金流上線才有真實數據，所以先做這塊。0800 值機桌面（同計畫書§4）使用者已先行做完，這次不重複處理。
  - **新增 `QcReportPage`**（`waferlock_crm.html`，NavBar「📊 品質與派工報表」）：
    - 型號 × 故障原因大類 交叉統計表（跨全部師傅，區別於既有「師傅報表」只看單一師傅）
    - 近 6 個月故障件數趨勢（純 CSS bar，未引入 Chart.js——專案目前完全沒用到圖表庫，不為單一報表新增依賴）
    - Top 故障原因排行（不分型號）
    - 師傅時段利用率：本月已排 bookings 格數 / 可用總格數（13格 × 當月天數），沿用 `SLOT_HOURS`/`REGIONS` 既有常數
    - 區域負載：本月各區（北/中/南）已排班次加總
  - 月份篩選與 completedDate 解析比對邏輯直接沿用 `WorkerReportPage` 已驗證過的寫法（Date 物件解析，不用字串範圍比較，避免重踩日期格式那次事故的坑）。
  - ✅ **端到端驗證（正式 Supabase，admin 帳號唯讀瀏覽，未寫入任何資料）**：登入後實際切換到新頁面，型號×故障原因交叉表、月趨勢、故障排行、師傅利用率、區域負載皆用真實資料正確渲染（本月1筆已結案：L376／電力電池問題；師傅時段利用率與區域負載數字對應真實 bookings），console 無錯誤。
  - 未動：營收儀表板、對帳報表（依賴金流§2上線）、客服績效（需先補「誰回的」歸屬邏輯）、B2B建案請款報表（billings流程未定，等內部確認清單21題）。
  - 影響檔案：`waferlock_crm.html`。

- 2026-07-03｜計畫書1（系統健檢報告）P2：Hash deep link 支援 hashchange。（Claude）
  - 背景：`waferlock_crm.html` 的 hash deep link（`#crm/{wfId}/{caseId}`）原本只在登入後＋db載入後解析一次（`hashApplied` ref 鎖住），頁面開著時改網址 hash 不會跳轉，必須整頁重新載入。計畫書1指出這是 0800 來電彈屏若沿用此機制的前置限制。
  - **修法**：把原本寫死在 `useEffect` 裡的解析邏輯抽成 `applyHash()`（`useCallback`），初次進入時仍保留 `hashApplied` ref 鎖住只跑一次的行為不變；新增第二個 `useEffect` 監聽 `window.addEventListener('hashchange',...)`，之後每次 hash 變化都會呼叫同一個 `applyHash()`，不用整頁重載即可跳轉到新的工單/客戶。
  - P2 其餘三項（移除 CaseDetailPanel 死碼、前端建置流程、測試基礎建設）依使用者決定留給廠商重建時處理：CaseDetailPanel 死碼移除本身也與 `CLAUDE.md` 通用規則「不能刪除任何現有函式」衝突，不會在這個階段刪除。
  - ✅ 驗證：本機伺服器載入，Babel 編譯無誤、console 無錯誤、登入畫面正常渲染；hash 導航行為需要登入態＋正式客戶資料才能完整測試，本輪未在有真實帳密的環境驗證，需人工實測「頁面開著時點第二個深連結」是否正確跳轉。
  - 影響檔案：`waferlock_crm.html`。

- 2026-07-03｜0800 值機桌面：通話中即時筆記 ＋ ACW 分類改三階段。（Claude）
  - 需求：使用者指出通話進行中就該讓專員填筆記（邊講邊記），ACW 只是補充/修改，不是從零開始；處理分類要改成三階段（比照客訴分類）。
  - **通話中即時筆記**：`callState` 新增 `note` 欄位，接聽（`answerCall`）時初始化為空字串；③ 中間互動面板的通話中卡片（`.cc-activecard`）新增筆記 `<textarea>`，`onChange` 即時寫回 `callState.note`。掛斷（`hangup`）時把這份筆記帶入 `acwPending.note`，⑤ ACW 面板的筆記欄初始值即為通話中已寫的內容，可繼續編輯，不是空白重來。
  - **ACW 分類改三階段級聯下拉**：新增 `PHONE_CAT` 常數——直接沿用 CRM 客訴單既有的 `COMPLAINT_CAT` 三層分類（電子鎖諮詢/電池問題/維修申請/保固查詢/操作疑問/其他，每個大類下有中類、每個中類下有細項清單），並在「其他」大類下新增「非業務來電」中類（打錯電話/騷擾測試/無聲來電），涵蓋客訴分類樹沒有的純電話場景。這正是落實計畫書2 §4.2 寫的設計要求：「ACW 狀態碼與客訴三層分類共用同一套類目，供合併統計」。原本的單一 8 選項 `ACW_CODES` 常數移除。
    - 三個 `<select>` 級聯：選大類清空中類/細項、選中類清空細項；未選大類不給送出（比照 CRM 客訴結案「主分類必選」慣例）。
    - 通話摘要寫入 `conversations` 時分類路徑格式：`大類 › 中類 › 細項`（省略未選的層級）。
  - ✅ **端到端驗證（正式 Supabase，真實資料）**：模擬來電並接聽後，通話中筆記欄可即時輸入；掛斷後 ACW 面板筆記欄正確承接同一份內容；三層下拉逐層測試（選「電池問題」→中類選項正確切換為耗電異常/無法充電/電池更換/低電量警告→選「耗電異常」→細項正確切換為待機耗電過快等5項），送出後 `conversations.last_msg` 正確顯示「分類：電池問題 › 耗電異常 › 待機耗電過快｜備註：客戶反應電子鎖耗電過快，正在確認電池型號」；全程 console 無錯誤。測試資料驗證後已刪除，正式環境確認無殘留。
  - 影響檔案：`waferlock_LINE.html`（`PHONE_CAT` 取代 `ACW_CODES`、`EightZeroZeroPage` 的 state/函式/JSX、新增 `.cc-livenote`/`.cc-acw-cats` CSS）。

- 2026-07-03｜0800 值機桌面原型（依計畫書2 §4.2 Genesys 五區塊規格，話務 mock）。（Claude）
  - 背景：`waferlock_LINE.html` 原本已有 NavBar「📞 0800 客服」分頁與 `EightZeroZeroPage` 元件，但該元件是**未完成且會報錯的半成品**——假設連線不存在的 `ws://localhost:8080` WebSocket 與不存在的 `RAG_SERVER_URL/auth/login` 登入 API，且誤用 `supabase`（SDK 本體）而非 `sb`（連線後的 client），執行時會直接壞掉；UI 也只有「來電號碼＋錄音鈕＋登入表單」，跟五區塊佈局規格對不上。使用者確認後直接取代，不保留舊邏輯。
  - **共用元件抽取**：把 `AgentPage`（LINE/FB/IG 客服台）原本內嵌的客戶 360 資訊面板（帳戶/保固/購買通路/案件紀錄縮影）抽成獨立元件 `CustomerInfoPanel({wfId})`，連同 `CASE_STATUS_LABEL`/`caseStatusLabel`/`openCaseInCrm` 一併提升為模組層級全域——這正是計畫書判斷「④ 右側客戶關聯資訊區大致已有」的具體實作，AgentPage 與新的 0800 頁面共用同一份查詢/顯示邏輯，不重複維護。
  - **`EightZeroZeroPage` 重寫為五區塊值機桌面**（① 頂部狀態列／② 左側導覽列／③ 中間互動面板／④ 右側客戶區／⑤ 底部 ACW，對應計畫書2 §4.2）：
    - ① 座席狀態切換（值勤中/離開/用餐/會議中/離線）＋通話中才顯示的話務控制列（計時器/靜音/保留/轉接/掛斷）
    - ② 通話紀錄列表 ＋「模擬來電」測試入口（輸入電話號碼模擬進線，比照既有「客戶LINE端模擬」的測試模式）
    - ③ 來電/通話狀態卡（響鈴中/通話中/保留中）
    - ④ 直接複用 `CustomerInfoPanel`；電話對到 0/1/多筆客戶時分三種畫面（查無資料提示／自動彈出／候選清單可點選——沿用「電話非唯一」的既有領域知識）
    - ⑤ 掛斷後強制進入話後處理：通話筆記（選填）＋ ACW 狀態碼（`ACW_CODES` 常數，必選才能送出），完成後寫入 `conversations`（新增 `platform:'phone'`，id 格式 `phone_<電話號碼>`，與既有 LINE `U...`/FB `fb_...`/IG `ig_...` 前綴並列，無需改資料庫 schema）
  - **架構原則**（比照 `mockPayment` 之於金流）：話務狀態機（振鈴/接聽/保留/轉接/掛斷）全部前端模擬，函式命名與資料落地邏輯已就緒，之後串接真實 CCaaS SDK（Genesys Embeddable Framework／Amazon Connect Streams）時只需替換話務控制函式內部實作。
  - ✅ **端到端驗證（正式 Supabase，真實資料）**：用真實電話 `0985688563` 模擬來電，**正式資料庫該號碼確實對到兩筆客戶**（同址不同棟號），候選清單正確觸發並可選取；接聽後右側正確顯示與 LINE 客服台一致的客戶 360 視圖；靜音/保留切換正確反映 UI 狀態；掛斷→未選 ACW 分類時送出被正確擋下→選分類後送出，`conversations` 表新增一筆 `id=phone_0985688563, platform='phone', wf_id=WF-2026-00001`，摘要格式正確；全程 console 無錯誤。測試資料驗證後已刪除，正式環境確認無殘留。
  - 影響檔案：`waferlock_LINE.html`（新增 `CustomerInfoPanel`／`ACW_CODES`／`AGENT_STATUS_LABEL`，重寫 `EightZeroZeroPage`，`AgentPage` 改呼叫共用元件，新增 `.cc-*` CSS）。

- 2026-07-03｜計畫書2 的 0800 章節升級：值機介面（Agent Desktop）設計規格。（Claude）
  - 使用者提供 Genesys Cloud 值機畫面的五大區塊說明（頂部狀態列/左側導覽列/中間互動面板/右側客戶關聯資訊區/底部互動摘要區），依此把 [`docs/計畫書2_擴張藍圖_金流報表0800.md`](docs/計畫書2_擴張藍圖_金流報表0800.md) §4 從「能力清單」升級為「介面設計規格」。
  - 新增 §4.2：五區塊 ASCII 佈局圖、逐區「Genesys 功能定義 × 現有系統對應 × 落差」對照表（關鍵發現：現有客服整合平台已是三欄雛形，④ 右側客戶區大致已有，①⑤ 為全新）、ACW 狀態碼與客訴三層分類同源的設計要求、整合模式 A/B 決策（建議 B：自建統一桌面＋嵌 CCaaS SDK）。
  - 原 §4.2 CTI → §4.3、方案比較 → §4.4（POC 驗證加第三項：softphone SDK 可嵌入性）、人員狀態 → §4.5；分期路線圖第3期補值機桌面；第3期驗收要點新增五區塊逐區驗收條件。
  - 影響檔案：`docs/計畫書2_擴張藍圖_金流報表0800.md`。

- 2026-07-03｜計畫書1（系統健檢報告）P1 項目修復（部分）。（Claude）
  - 背景：接續 P0 之後的 P1 項目，本輪完成案號搜尋、workers 常數改讀DB、LINE端Realtime增量化、師傅端新單提示、日期格式統一（含正式資料遷移腳本）；PRODUCTS/CHANNELS/LOCKSMITHS 因資料庫無對應資料表暫緩。
  - **案號搜尋接通已登錄客戶**：`waferlock_crm.html` 的 `doSearch()` 原本比對條件（姓名/電話/客編/序號/地址）不含案號，客服拿工單號查客戶永遠只會落到「未登錄」分支。改為：先用案號比對出案件的 `wfId`，若該 `wfId` 對應到已登錄客戶，直接併入已登錄結果，不再產生假的未登錄候選卡片。
  - **workers 常數改讀 DB（LINE端）**：`waferlock_LINE.html` 原本從未讀取 `workers` 資料表，完全依賴本地硬編碼 `WORKERS`（缺 region/type、多餘的 status 欄位，且新增師傅不會反映）。`loadFromSupabase()` 補上 `sb.from('workers').select('*')`，4 處使用點（緊急廣播篩選、派工選擇、案件卡片師傅姓名、下拉選單）改為 `db.workers` 優先、`WORKERS` 常數僅作為載入前的 fallback，比照 `waferlock_crm.html` 既有寫法。PRODUCTS/CHANNELS/LOCKSMITHS 資料庫沒有對應資料表（只有 workers 有），改讀DB需要先建新表，範圍超出本次「接通既有表」的估算，暫不處理。
  - **LINE端 Realtime 改增量**：`waferlock_LINE.html` 原本任何一張表變動就 `loadFromSupabase()` 整顆重拉（8張表全量），比照 `waferlock_crm.html` 已於 2026-06-26（commit `2a8b055`）修好的方案，改為依 `payload.table`/`eventType` 只更新對應的單筆資料（customers/repairs/installs/complaints/conversations/workers/config 皆已覆蓋）。
  - **師傅端新單提示**：釐清一個報告描述有出入的地方——`waferlock_tech.html` 其實從最早版本（commit `bb03280`）就有 20 秒輪詢刷新（`setInterval(loadCases,20000)`），並非報告所述「完全無 Realtime、接單靠運氣」，只是刷新後沒有主動提示。這次補上：`prevMineIds` 追蹤上一輪「進行中」案件（維修+安裝），新出現的 id 觸發 `notify()`（畫面頂部 toast + 震動），不再需要師傅自己盯著畫面才會發現新派工。
  - **日期格式統一為 YYYY-MM-DD**：`waferlock_crm.html`/`waferlock_tech.html`/`waferlock_LINE.html`/`liff-bind.html` 的 `today()`/`todayStr()` 原本用 `toLocaleDateString('zh-TW')` 輸出無補零的 `M/D/YYYY`，是 2026-07-01 師傅報表篩不到資料那次事故的根因，四檔全部改輸出 ISO `YYYY-MM-DD`（`waferlock_crm.html` 直接改成 `todayISO` 的別名，該函式本來就已存在且輸出正確格式，只是沒被 `today()` 使用）。新增 [`supabase_date_format_migration.sql`](sql/supabase_date_format_migration.sql)，把正式資料庫既有的 `M/D/YYYY` 資料轉為 `YYYY-MM-DD`（`customers.reg_date`/`install_date`、`installs.created_date`/`completed_date`、`repairs.date`/`completed_date`、`complaints.date`/`closed_date`），用正規表示式只命中舊格式資料列，可重複執行不會誤傷已是 ISO 格式的資料。
  - ⚠️ **範圍刻意排除**：`repairs.created_at`/`complaints.created_at` 是 `today()+' '+now()` 組成的日期+時間文字，`now()` 用 zh-TW 12小時制（含「上午/下午」），格式比純日期欄位複雜很多，且不是報表 `.gte()` 篩選會踩到的欄位（未在本次事故範圍內），這次沒有動，遷移腳本內有註明原因。
  - ⚠️ **部署前置條件**：[`supabase_date_format_migration.sql`](sql/supabase_date_format_migration.sql) 需人工在 Supabase SQL Editor 執行（腳本內附「執行前檢查」與「執行後驗證」的 SELECT 語句，建議先跑一次確認命中筆數）。不執行也不會讓功能壞掉（新資料已經是正確格式），只是舊資料仍會有字串比較篩不到的風險，等同於原本的 bug 尚未根治。
  - ✅ 驗證：四個檔案皆已用本機伺服器載入，Babel/JS 編譯無誤、console 無錯誤、頁面正常渲染。**未在正式環境做端到端測試**（doSearch/workers讀取/Realtime增量/新單提示 皆需要正式資料與多分頁/多裝置情境才能完整驗證，靜態環境無法模擬）。
  - 未動：密碼明文/RLS全開（P0 已標記為需與廠商時程協調的獨立項目）、PRODUCTS/CHANNELS/LOCKSMITHS 改讀DB（需先建表）。
  - 影響檔案：`waferlock_crm.html`、`waferlock_LINE.html`、`waferlock_tech.html`、`liff-bind.html`、新增 `supabase_date_format_migration.sql`。

- 2026-07-03｜計畫書1（系統健檢報告）P0 純前端項目修復。（Claude）
  - 背景：`計畫書1_系統健檢報告.md` 盤點出的 P0 修復項，本輪只做純前端＋可獨立部署的部分，密碼雜湊／RLS 收斂另議。
  - **移除「重置DB」誤觸風險**：`waferlock_crm.html` 的重置按鈕改為僅 `isAdmin` 可見，且 `confirm()` 換成需輸入「清空資料庫」四字的 `prompt()` 雙重確認；`waferlock_LINE.html` 因整頁無登入/角色機制、無法做權限區隔，直接移除該按鈕。
  - **LINE 端欄位映射補齊**：`waferlock_LINE.html` 的 `repairToDb`/`dbToRepair`（36→67 欄）、`installToDb`/`dbToInstall`（25→42 欄）改為與 `waferlock_crm.html` 完全一致，補上 `txn_id`/`paid_at`/`sched_*`/`warranty_judgment`/`history`/`tenant_type` 等欄位，消除跨端 upsert 舊值覆寫風險。
  - **wf_id 產號統一走 RPC**：新增 [`supabase_wf_id.sql`](sql/supabase_wf_id.sql)（`next_wf_id()`，`wf_counters` 表原子遞增，格式統一為 `WF-YYYY-NNNNN`）。`waferlock_crm.html`（4處：安裝建單、報修建單、手動建客戶、地址歸戶批次工具）、`waferlock_LINE.html`（LINE綁定建新會員）、`liff-bind.html`（LIFF綁定建新會員）全部改呼叫 `sb.rpc('next_wf_id')`，取代原本三種各自的本地格式/計數邏輯。
  - **簽收單號統一走 `next_case_no`**：`waferlock_crm.html` 的 `genReceiptNo()`（`SR-YYYYMMDD-隨機4碼`）移除，改用 `sb.rpc('next_case_no',{p_prefix:'SR'})`，與 `waferlock_tech.html` 已用的原子日流水格式（`SR202607030001`）一致。
  - ⚠️ **部署前置條件**：[`supabase_wf_id.sql`](sql/supabase_wf_id.sql) 必須先在 Supabase SQL Editor 執行，否則所有新客戶建立流程會因 `next_wf_id` RPC 不存在而失敗（無 fallback，屬刻意設計——寧可顯性報錯也不要重新退回舊的不安全產號邏輯）。
  - ✅ 驗證：三個檔案皆已用本機伺服器載入，Babel 編譯無誤、console 無錯誤、頁面正常渲染；因涉及正式資料庫寫入（新建客戶/工單），**未在正式環境做端到端測試**，需在 SQL 遷移執行後由人工實測一次建立客戶／建立工單／LINE綁定三條路徑。
  - 未動：密碼明文/RLS全開（報告列為需與廠商時程協調的獨立項目）、日期格式統一、常數改讀DB等 P1/P2 項目。
  - 影響檔案：`waferlock_crm.html`、`waferlock_LINE.html`、`liff-bind.html`、新增 `supabase_wf_id.sql`。

- 2026-07-03｜LINE 客服整合平台「客戶資訊」面板串接 CRM 案件紀錄。（Claude）
  - 背景：客戶資訊面板原本已經是即時同步的真實資料（`db.customers`/`db.repairs`，與 CRM 共用同一顆 Supabase），但只顯示「報修紀錄」最多 2 筆、且無法互動；使用者想要維修/安裝/客訴三種案件都整合呈現，並能點開看更詳細的紀錄。
  - **資料層補洞**：`waferlock_LINE.html` 的 `repairToDb`/`dbToRepair`、`installToDb`/`dbToInstall` 長期沒跟上 CRM 端新增的欄位（金額、付款、型號、師傅），這次補上 `charges`/`chargeTotal`/`paymentMethod`/`paid`/`faultModel`（維修）、`workerId`（安裝）映射。
  - **「📁 案件紀錄」統一區塊**：合併維修/安裝/客訴三種類型（依日期新到舊排序），每筆顯示類型標籤、狀態徽章（依類型查表 `CASE_STATUS_LABEL`）、日期、標題/問題描述、師傅姓名（如有）、收費金額（如有）；超過 2 筆時可點標題或「展開全部」收合/展開。
  - **案件詳情連結**：每筆卡片有「🔗 查看詳情 →」，用 `window.open('waferlock_crm.html#crm/{wfId}/{caseId}','_blank')` 開新分頁，直接沿用 CRM 既有的 hash deep link 機制，銜接到完整的單張案件視圖（含上一輪新增的品項明細/驗收紀錄/照片簽名等）。
  - ✅ 驗證：真實資料客戶（程心妤示範對話）確認案件正確顯示型別/狀態/日期/師傅/連結，console 無錯誤；展開/收合邏輯與已驗證的資料抓取共用同一陣列，風險低（純條件渲染），依程式碼審閱確認正確。
  - 影響檔案：`waferlock_LINE.html`。

- 2026-07-03｜單張案件卡片內容完整化（維修＋安裝）。（Claude）
  - 需求：使用者展示維修單卡片畫面，希望「維修或安裝的紀錄、品項等等」都完整呈現在卡片上，而非只有摘要。
  - **收費品項明細**：新增共用函式 `renderChargesDetail(charges)`，逐筆列出品名×數量@單價=金額（贈品標示）＋合計，取代原本只顯示 `chargeTotal` 總額；維修、安裝卡片皆套用。
  - **維修卡片新增「🧾 結案紀錄」區塊**（僅 `status==='done'` 顯示）：保固判定（保固內免費/保固外收費/保內不保固＋原因）、工務備註、稽核備註、結案日期——這些欄位原本都已寫入資料庫，只是卡片沒有拿出來顯示。
  - **安裝卡片補齊到與維修卡片同等級**（原本明顯陽春，兩者不對稱）：新增付款摘要列、收費品項明細、「🔬 驗收與教學紀錄」區塊（鎖舌對位/自動上鎖/解鎖方式測試三項確認＋教學確認＋工務備註）、異動紀錄展開（🕓 按鈕，原本只有維修卡片有）。
  - **照片／簽名查看**：新增 `PhotoSignatureModal` 元件（縮圖 grid，點擊可全螢幕放大），不內嵌在卡片預覽裡；**權限卡控**——按鈕僅 `currentUser.role==='admin'` 時顯示，沿用既有角色機制，未新建權限系統。
  - ✅ **端到端驗證（正式 Supabase，使用者已明確同意寫入測試資料）**：建立獨立測試客戶＋測試安裝單（含完整 charges/verify/taught/photos/signature/history），驗證安裝卡片全部新增區塊正確渲染；同時以真實維修單 `R202606300001` 確認無回歸、品項明細與結案紀錄正確顯示；驗證非 admin（客服-小美）看不到照片按鈕、admin 可開啟並看到真實照片與簽名筆跡、放大檢視與關閉皆正常。測試資料驗證後已完整刪除，正式環境確認無殘留。
  - 影響檔案：`waferlock_crm.html`（`renderChargesDetail`、`renderRepairCard`、`renderInstallCard`、新增 `PhotoSignatureModal`、`viewingMedia`/`isAdmin` state）。

- 2026-07-02｜維修單／安裝單新增「轉單改派」機制（含異動紀錄）。（Claude）
  - 需求：工單確定派工後，若工程師臨時無法前往，需能改派其他師傅，且任何異動都要留紀錄。
  - 做法（復用既有派工／排程 Modal，未另開元件）：
    - 維修 `dispatched` 卡片新增「🔁 轉單改派」按鈕 → 重開派工 Modal 選新師傅／時段；安裝端沿用既有「改排程」，已指派師傅時按鈕改顯示「🔁 轉單／改排程」。
    - **修 bug**：`doRepairDispatch` 原本未先 `releaseSlots` 就 `bookSlots`，轉單會在工務時刻表殘留舊師傅孤兒佔格；已補 `releaseSlots`（安裝端原本就有）。
    - **修隱藏資料遺失**：`openRepairDispatch` 原本重開會清空 `dispChargeRows`，導致轉單覆寫時把既有預估品項清掉；改為從 `r.charges` 帶回。
    - 轉單原因欄位：Modal 內偵測「已有指派師傅」時才顯示，顯示目前指派師傅＋原因輸入框（選填）。
    - 異動紀錄自動記 from→to：轉單記 `🔁 轉單：原 A → B｜時段｜原因`；同師傅改時間記 `🔁 改期`；首次派工／排程維持原描述。
    - 客戶 LINE 通知沿用 Modal 既有推播（轉單確認時自動通知住戶新師傅／時段）。
  - 影響檔案：`waferlock_crm.html`（`doRepairDispatch`/`doInstallSchedule`/`openRepairDispatch`/`openInstallSchedule`、維修與安裝卡片按鈕、兩個排程 Modal、新增 `dispReason` state）。

- 2026-07-01｜修正師傅報表篩不到資料（日期格式不匹配）。（Claude）
  - **問題**：使用者實測結案後「我的報表／師傅報表」皆顯示 0 筆，誤以為是即時性問題。實查為 bug：`completed_date` 由既有 `today()`（`toLocaleDateString('zh-TW')`）產生，格式為 `M/D/YYYY`（如 `2026/7/1`，無補零、斜線分隔），但報表月份篩選用 `.gte()/.lt()` 字串範圍比較，誤假設為 `YYYY-MM-DD` ISO 格式，兩種格式字典序不一致，篩選條件恆為假。
  - **修法**：`waferlock_tech.html` `loadReport()` 改為 SQL 層只依 `worker_id`/`status='done'` 抓回（`limit(500)`），月份篩選改在前端用 `new Date()` 解析後比對年/月（`inReportMonth`）；`waferlock_crm.html` `WorkerReportPage` 同步改為 `inMonth()` 用 Date 物件比較，取代原本的字串比較。
  - **順便修正**：師傅端切換到「我的報表」分頁的快取邏輯（`!reportData.length` 導致曾經有資料後就不再重新查詢），改為每次切入分頁皆重新查詢最新資料。
  - ✅ 驗證：用使用者實測產生的真實案件 `R202607010001`（型號 L376、故障「電力/電池問題／鋰電池更換」）確認修正後「我的報表」與 CRM「師傅報表」皆正確顯示 1 筆，型號故障率排行與工單細項內容正確；兩端 console 皆無錯誤。
  - 影響檔案：`waferlock_tech.html`、`waferlock_crm.html`。

- 2026-06-30｜維修結案三階段 QC 分類 ＋ 品項連動師傅端 ＋ 師傅報表。（Claude）
  - **背景**：舊系統匯出範例（使用者提供 Excel）顯示「處理說明」欄把故障原因與處理動作混寫成一段自由文字，QC 無法依型號統計故障率。另發現客服建單/派工時設定的收費品項，師傅端結案永遠從空白重打；師傅完全沒有報表可看案件數/應收帳款。
  - **① 三階段 QC 分類**：新增 `supabase_repair_qc.sql`（`repairs` 加 `fault_model`/`fault_reason_major`/`fault_reason_detail`/`handling_result`）。`waferlock_tech.html` 結案表單改為「型號確認→故障原因（大類+細項）→處理過程」三階段下拉選單。
    - 分類設計**不用「系列」猜測**，改依使用者提供的《WAFERLOCK全型號知識庫》真實規格：新增 `MODEL_ATTRS` 常數記錄 27 個型號的開鎖方式/電源類型/特殊功能，故障細項依屬性動態組合（如僅鋰電池型號顯示鋰電池相關故障、僅有 App 功能的型號才顯示 App/連線問題大類）；`R400`（網關配件）與 `WAFERKEY`（純軟體）不套用門鎖機構/電池架構，改用各自專屬的連線/帳號問題選項。
    - 型號預設帶入 `customers.product_id`，師傅可確認/修正。
  - **② 品項連動師傅端**：`waferlock_tech.html` `openReport()` 原本 `charges=[]` 強制清空，改為帶入 `rep.charges`（客服建單/派工時已設定的品項），師傅可編輯/追加而非重打。
  - **③ 隱藏異動紀錄按鈕**：⚠️ 過程中發現 `CaseDetailPanel` 元件（原本設計要放異動紀錄的地方）**從未被任何頁面掛載，是既有死碼**（非本次造成）。改為在真正使用中的 `renderRepairCard`（單張案件視圖）加低調小按鈕「🕓 異動紀錄（N）」，點擊展開/收合，未動 `CaseDetailPanel` 本身的既有內容。
  - **④ 師傅報表**：`waferlock_tech.html` 新增「📊 我的報表」（師傅只看自己），`waferlock_crm.html` 新增「👷 師傅報表」管理頁（可切換師傅＋月份）。皆含：案件數、應收帳款（已收/待收分列）、平均處理時效（報修日→結案日）、型號故障率排行、未收款/待對帳清單、工單細項。
  - ✅ **端到端驗證（正式 Supabase）**：SQL 四欄位寫入讀回清除通過；`waferlock_tech.html` 實測型號下拉正確帶出對應故障大類/細項（L376 含 App 大類、鋰電池限定細項）、品項連動正確帶入既有收費品項（模擬「更換機板$3000」重開結案畫面仍在）；`waferlock_crm.html` 實測真實案件（`R202606300001`）異動紀錄按鈕正確顯示筆數並可展開/收合；兩端皆無 console 錯誤。**測試僅用記憶體模擬資料與唯讀查詢，未寫入任何測試髒資料到正式環境。**
  - 影響檔案：新增 `supabase_repair_qc.sql`；`waferlock_tech.html`、`waferlock_crm.html`。同步更新 [`內網遷移規格需求書.md`](docs/內網遷移規格需求書.md) §7/§8，供未來新系統參考。

- 2026-06-30｜Facebook Messenger / Instagram DM 雙向串接。（Claude）
  - 新增 Supabase Edge Function `meta-webhook`（收訊）：Meta GET 握手驗證、HMAC-SHA256 簽章驗證、解析 `entry.messaging`、寫入 `conversations`（id 格式 `fb_<PSID>` / `ig_<IGSID>`）、首則自動回覆。
  - 新增 Supabase Edge Function `meta-push`（發訊）：接收 `{to, text}`，解析 `fb_`/`ig_` 前綴取真實 recipient id，呼叫 Meta Graph API v19.0 `/me/messages`。
  - 前端 `waferlock_LINE.html`：新增 `pushMeta` 函式；`sendMsg` 改依 `active.platform` 路由——`facebook`/`instagram` 走 `meta-push`，`line` 維持 `line-push`。
  - Supabase Secrets 需設定：`META_APP_SECRET`、`META_PAGE_ACCESS_TOKEN`、`META_VERIFY_TOKEN`（已設定）。
  - ✅ 端到端驗證（2026-06-30）：FB Messenger 用戶傳訊 → 收件匣出現 FACEBOOK 標籤對話 → 客服回覆 → 用戶 FB Messenger 收到訊息，全流程正常。
  - ⚠️ 限制與待辦：
    - App 目前在**開發模式**，只有 App 角色成員（管理員/開發人員/測試人員）的訊息才能觸發 webhook；正式對外需通過 Meta App Review（申請 `pages_messaging` 權限）。
    - Instagram DM 串接尚未設定 Webhook（IG 設定頁面同一個網址，待補）。
    - `meta-push` 回覆限制：開發模式下只能回覆 24 小時內有互動的用戶（Meta 標準訊息視窗政策）。
  - 影響檔案：新增 `supabase/functions/meta-webhook/index.ts`、新增 `supabase/functions/meta-push/index.ts`、修改 `waferlock_LINE.html`（`pushMeta` + `sendMsg` 路由）。

- 2026-06-25｜LINE 客服 AI 主控台：影片辨識（延伸既有圖片 Vision）。（Claude）
  - 釐清誤解：模型字串不在 `waferlock_LINE.html`，在 RAG 伺服器 `RAG資料庫/rag_server.py`；現用 `gemini-2.5-flash`（Flash 系列最新，本就支援影片，無「Gemini 3.5 Flash」此版本）。影片辨識卡關不在模型，而在程式只收單張圖片。
  - 範圍：**僅做網頁主控台**操作者上傳影片辨識（沿用相機鈕流程）。LINE 用戶自傳影片自動辨識未做（webhook 未下載媒體內容，且 localhost RAG 雲端連不到，屬另案）。
  - 前端 `waferlock_LINE.html`：file input `accept` 加 `video/*`；`handleImageSelect` 標記 `isVideo`；`callAI` 依 `isVideo` 改送 `video_base64`/`video_mime_type`；預覽與聊天氣泡對 `data:video` 改用 `<video>` 渲染；歷史訊息文案區分照片/影片。
  - 後端 `rag_server.py`：`ChatRequest` 加 `video_base64`/`video_mime_type`；`/chat` 組裝影片 `Part.from_bytes`（>20MB 印警告，inline 上限，更大需改 File API）；Vision system prompt 與無文字提示語涵蓋影片。
  - ✅ 驗證：`rag_server.py` py_compile 通過；`waferlock_LINE.html` 本機預覽 Babel 編譯無錯、5 處改動原始碼確認到位。**實際送影片給 Gemini 的端到端待你在本機啟動 rag_server.py（需 API Key）＋ 一段短片實測。**
  - 影響檔案：`waferlock_LINE.html`、`RAG資料庫/rag_server.py`（後者在本專案資料夾外）。

- 2026-06-26｜影片辨識上線後修正（三項，含一支既有潛在 bug）。（Claude）
  - **① CSS 影片爆版修正**：`.img-preview-wrap img`／`.mb img.chat-img` 選擇器只寫死 `img`，`<video>` 沒套到尺寸 → 預覽縮圖與聊天氣泡用原始解析度撐爆。選擇器擴及 `video`（預覽 44px、氣泡 max-width 160px）。前端，已隨 commit `8040646` 推上正式。
  - **② Vision prompt 修正**：原 prompt 只「辨識型號」，辨識完回到漏斗式反問去問「遇到什麼狀況」，但狀況已在影片畫面裡。改為「型號＋狀況一起判讀」，明令客戶已用影片/照片詢問故障時**不得反問狀況**，直接就畫面研判異常＋給原因與處理建議。`rag_server.py`，需重啟生效。
  - **③ 既有潛在 bug：只傳媒體不打字會 400**：`/chat` 在 `message` 為空時直接 `raise 400 Empty message`，使「純傳圖片/影片（沒打字）」被擋（前端顯示「系統暫時無法回應」）。改為「有圖片或影片時放行」，交由 Vision 模式以 `media_hint` 處理。`rag_server.py`，需重啟生效。
  - ✅ 實測：影片可辨識型號＋研判問題（L310 / L3XX 系列）；圖片爆版已修；`rag_server.py` py_compile 通過。
  - ⚠️ 限制與待辦：
    - `rag_server.py` 在本 repo 外，修正靠**人工重啟**生效，非 git 部署。
    - AI 目前只在「有跑 rag_server 的那台本機」可用（`RAG_SERVER_URL` 寫死 `localhost:8000`）。正式對外上線需：把 rag_server 部署到**公開 HTTPS** 伺服器 ＋ 改 `RAG_SERVER_URL`。
    - 影片/照片診斷精度受限於 `vision_model_guide.md` 只有外觀特徵、無「故障徵象→原因→處理」對照；補故障知識庫比換模型更能提升準確度。
  - 影響檔案：`waferlock_LINE.html`（CSS）、`RAG資料庫/rag_server.py`（prompt＋空訊息守門）。

- 2026-06-26｜客訴流程第二輪：追蹤時間補時分 ＋ 操作改下拉選單 ＋ 首頁雙區塊。（Claude）
  - **追蹤時間補時分**：`followModal` 的追蹤日期欄位從 `type="date"` 改為 `type="datetime-local"`（24 小時制），label 改為「下次追蹤時間（24 小時制）」；卡片顯示改為 `YYYY-MM-DD HH:MM` 格式（`.replace('T',' ')`）。
  - **操作改下拉選單**：移除客訴卡片上的「結案」按鈕（右上角）、「🔍 標記追蹤」「🔧 建立派工」按鈕（底部）；非結案狀態統一改為「— 選擇處理方式 —」下拉選單，包含：📅 待追蹤 / 🔧 維修案件 / 🛠️ 裝機案件 / ✅ 線上結單，選完自動歸位。
  - **新增 `sourceComplaintType` state**：從客訴卡片觸發「維修案件」開 `WorkOrderForm` 時傳 `'repair'`，觸發「裝機案件」時傳 `'install'`，讓 WorkOrderForm 預填對應類別；`onClose` 後重置為 `'repair'`。
  - **首頁客訴雙區塊**：點側邊欄「📞 客訴件」後，主列表分為「📋 未處理列表」（pending）與「🔍 待追蹤列表」（following）兩個標題區塊，皆套用部門篩選；移除原本「🔍 追蹤中客訴 N 件切換功能」（已整合進主列表）。
  - **側邊欄客訴計數調整**：「📞 客訴件」計數從只計 `pending` 改為 `pending + following` 合計。
  - 影響範圍：`waferlock_crm.html` 僅修改 `renderComplaintCard`、`followModal` JSX、`caseModal` JSX、`HomePage` 主列表渲染、側邊欄計數。

- 2026-06-26｜師傅端 pushCustomer 改從 conversations 查真實 LINE uid。（Claude）
  - 問題：`customers.line_uid` 儲存的是舊測試值（`live_user`）而非真實 `U...` uid，導致 `pushCustomer` 以 `custMap[wfId]?.line_uid` 取 uid 後 `startsWith('U')` 不符，靜默跳過推播。
  - 修法：`pushCustomer` 改先 `await sb.from('conversations').select('id').eq('wf_id',wfId)`，從中找 `id.startsWith('U')` 的那筆取得真實 uid，找不到才 fallback 回 `custMap[wfId]?.line_uid`。
  - ✅ 端到端驗證（2026-06-26 用戶實機）：師傅接案 R202606250007，LINE App 收到「壹立工班1（仁哥）已接單，預計今天 15:05–15:25 抵達」，LINE 推播全流程正常。
  - 影響檔案：`waferlock_tech.html`。

- 2026-06-26｜CRM Realtime 訂閱改增量更新，不再全量重拉。（Claude）
  - 問題：每次 Supabase `postgres_changes` 事件都觸發 `loadFromSupabase()`，全量拉 10 張表，是造成 5GB egress 異常的主因之一（另一主因已由 beba149 的照片遷移解決）。
  - 修法：`useEffect` 改為讀 `payload.table`/`eventType`/`new`/`old`，依 INSERT/UPDATE/DELETE 只更新 `db` state 中受影響的單一筆記錄。同步更新 `prevDbRef.current` 以維持 diff 精確性。
  - 影響範圍：`customers`、`repairs`、`installs`、`complaints`、`conversations`、`bookings`、`workers`、`groups`、`projects`、`config` 全 10 張表皆已涵蓋。
  - ✅ 已推正式環境（commit 2a8b055）。
  - 影響檔案：`waferlock_crm.html`。

- 2026-06-25｜師傅端推播改「雙軌」：寫進客戶對話 ＋ 發真實 LINE。（Claude）
  - 問題：師傅接案後客戶沒收到 ETA 通知。診斷出兩層原因：① 測試客戶（Fish=`live_user`、程心妤=`demo_user_1`）綁的是假 uid 非真實 `U...`，真實 LINE 推播本就發不出去；② **師傅端推播只走真實 LINE API、不寫 `conversations`**，對比 LINE 客服端建單會寫對話——導致連 LINE 模擬頁/CRM 對話都看不到師傅通知（只有 CRM 維修卡片 `eta_text` 欄看得到）。
  - 修法：`waferlock_tech.html` 推播改雙軌。新增 `logToConversation(wfId,text)`（依 wf_id 找該客戶所有對話、append 一則 bot 訊息）與 `lineApiPush(uid,text)`（uid 為 `U` 開頭才 invoke line-push）。`pushCustomer`/`pushByPhone` 改為「先寫對話、再發真實 LINE」，回傳 `{logged,pushed}`。接案 `confirmAccept`、維修結案、安裝完工三處依結果提示：未發 App 但已記錄對話時明確告知「已記錄到對話，此客戶未綁真實 LINE，未發 App 推播」。
  - 效果：師傅接案/結案/完工通知，現在 LINE 模擬頁住戶端＋CRM 對話都看得到；真實綁定客戶（`U...`）另發 LINE App。
  - ✅ 驗證：師傅端載入無 console 錯誤、四函式定義正確；實測 `logToConversation('WF-2026-00002',…)` 寫入 conv_live(18→19)＋Ub483(25→26) 兩對話後正確還原、無殘留。
  - ⚠️ 真實 LINE App 推播（line-push function＋`LINE_CHANNEL_ACCESS_TOKEN`）仍需用**真實綁定 LINE 的帳號**實測收訊（目前測試客戶皆假 uid，模擬頁可見對話即代表雙軌的「寫對話」這條已通）。
  - 影響檔案：`waferlock_tech.html`。

- 2026-06-25｜報修工單編號統一 ＋ ETA/推播顯性化。（Claude）
  - 背景：報修工單編號三種格式並存（標準 `R+YYYYMMDD+4碼`、`R-時間戳`、`R-001`），元兇是 LINE 端 `createCase` 用 `R-'+Date.now()` 沒走 `next_case_no` RPC；使用者要「單一規格」以利改網址即鎖定工單（deep link）。另：ETA 接案推播送出後客戶沒收到。
  - **編號統一**：`waferlock_LINE.html` `createCase` 改 async＋`next_case_no('R')`（與 CRM `submitRepair`、`liff-bind.html` 一致），並補齊缺漏欄位（`contactName/contactPhone/createdBy/createdAt/slaLevel/handlingUnit/warrantyIn/auditNote/history`）對齊規格，避免 CRM 顯示空欄。deep link 解析無需改（本就 id 精確比對）。
  - **推播顯性化**：診斷發現推播沒到是因測試客戶 `line_uid`（`live_user`/`demo_user_1`）非真實 `U...`，推播函式 `startsWith('U')` 不符就靜默跳過。`waferlock_tech.html` `pushCustomer`/`pushByPhone` 改回傳是否送出（`await`＋try/catch）；接案 `confirmAccept`、維修結案、安裝完工三處依結果，未送出時 `alert` 明確告知「客戶未綁 LINE，未發送通知，請改電話聯繫」，不再靜默。
  - **清測試資料**（使用者確認全刪）：刪除 8 筆非標準編號工單（皆測試/demo：Fish=live_user、程心妤=demo_user_1、七七、王妞妞，問題欄為「123/壞掉/報修」等），連動清掉 `customers.repair_ids` 引用（3 筆）與 `bookings`（3 筆）；刪除前已備份至 `_photo_backup/predelete_*`（不進版控）。
  - ✅ 驗證（正式 Supabase）：`next_case_no` 產生標準格式正確；清理後 `repairs` 剩 5 筆**全部標準格式**、零孤兒 `repair_ids`/`bookings`；deep link `#repair/R202606250003` 仍可鎖定；三檔（LINE/tech/crm）載入無 console 錯誤。
  - ⚠️ 未做：LINE 端「點 UI 真實建單」端到端（需 active 對話）——已驗 RPC＋語法＋與 CRM 同模式，下次真實建單即為標準格式。line-push function 本身需用**真實綁定 LINE 的帳號**實測收訊（目前測試客戶皆假 uid）。
  - 影響檔案：`waferlock_LINE.html`、`waferlock_tech.html`。

- 2026-06-25｜根治 CRM 開頁慢：施工/簽收照片改存 Supabase Storage。（Claude）
  - 🔴 **前置（需人工，已完成）：在 Supabase SQL Editor 執行 [`supabase_storage.sql`](sql/supabase_storage.sql)** 建 `work-photos` public bucket＋讀寫 policy。
  - 問題定位：開 CRM 慢與連線無關，是照片 base64 塞進資料列——`repairs` 11 筆=12.9MB、`installs` 9 筆=8.1MB，`loadFromSupabase` 一次 `select('*')` 拉 **21MB**、耗時 **6.9 秒**；且 CRM 根本沒顯示這些照片（只寫不讀），等於每次白拉。
  - 修法：照片改存 Storage，資料表 `photos` 只留 public URL 陣列。兩檔各加共用 `uploadPhotos(files,folder)`（上傳 `work-photos/<repairs|installs>/<id>/...`，回傳 URL）。`waferlock_crm.html` `PhotoUpload` 改上傳 Storage（含「上傳中」狀態）、`WorkOrderModal` 傳 folder；`waferlock_tech.html` 維修 `onPhotos`、安裝上傳兩處同改。`signature` 簽名（全表 25–35KB）依計畫暫留 base64（影響極小）。
  - 資料層/載入不需改：`photos` 仍是陣列、`select('*')` 自然瘦身。
  - 既有照片一次性遷移：base64→Storage→URL（repairs 5 張、installs 4 張，0 失敗，冪等可重跑）；遷移前已備份至本機 `_photo_backup/`（已加 `.gitignore`，不進版控）。
  - ✅ **端到端驗證（正式 Supabase）**：上傳測試取得可開啟 public URL（200/image/png）；遷移後重量測 `loadFromSupabase` **6939ms→328ms**、總下載 **21MB→127KB**（repairs 49KB、installs 32KB）、無 base64 殘留；CRM 與師傅端 `uploadPhotos` 皆實測上傳成功；測試物件已清除、正式環境無殘渣。
  - 影響檔案：`waferlock_crm.html`、`waferlock_tech.html`、新增 `supabase_storage.sql`、新增 `.gitignore`。

- 2026-06-25｜維修/安裝案件「專屬網址」產生端補齊（deep link 缺口）。（Claude）
  - 問題：Hash deep link 的「解析端」早已支援 `#repair/R-xxx`、`#install/IW-xxx`（開啟連結能定位＋高亮案件），但「產生端」缺漏——系統內唯一更新網址處只有「選中客戶→`#crm/WF...`」，導致點開維修案件排程時網址仍停在客戶層級，使用者**無從取得案件專屬網址**。
  - 修法：`CRMPage` 新增單一 `useEffect`，監聽 `dispatchRepair`/`schedInstall`/`sel`——開啟維修排程→網址自動變 `#repair/<id>`、開啟安裝排程→`#install/<id>`、關閉後還原成 `#crm/<wfId>`。一個 effect 涵蓋開啟/關閉/還原，不需在 5～6 個關閉點各別處理。
  - ✅ 本機驗證：開維修排程→網址自動變 `#repair/R202606250003`（與排程畫面案件一致），關閉→還原 `#crm/WF62247000`；無 console 錯誤。安裝側為同 effect 對稱分支（當下無可排程安裝單可即時點測，未建假資料污染正式環境）。
  - ⚠️ 已知缺口：已結案案件無排程按鈕→目前仍只有客戶網址；若需「每個案件（含已結案）都能複製連結」可後續在案件卡片加「🔗 複製連結」鈕。
  - 影響檔案：`waferlock_crm.html`。

- 2026-06-25｜UI 修正：送出按鈕被 NavBar 遮蔽問題（三處）。（Claude）
  - `WorkOrderForm`（新建工單 modal）：改為 flex column 結構，表單內容可捲動，「取消 / 建立工單」按鈕固定在 modal 底部，上方加分隔線。
  - 維修排程派工 fullsheet：底部 `fs-body` 末端新增「取消 / 確認派工＋推播」重複按鈕（`fs-top` 頂部按鈕被 NavBar z-index 蓋住）。
  - 安裝排程 fullsheet：同上，底部新增「取消 / 確認排程」重複按鈕。
  - 影響檔案：`waferlock_crm.html`。

- 2026-06-25｜Hash URL Deep Linking：每個工單/客戶可分享直接連結。（Claude）
  - URL 格式：`#crm/WF12345678`（客戶主檔）、`#repair/R-xxx`（維修工單）、`#install/IW-xxx`（安裝工單）。
  - 登入後 + db 載入後自動解析 hash，找到對應工單/客戶 → 跳 CRM + 高亮工單卡片（藍框，3 秒後消失，自動捲動定位）。
  - 選中客戶時 `window.history.replaceState` 更新 URL hash；切換 NavBar 頁籤時清除 hash。
  - 影響檔案：`waferlock_crm.html`（App state、Context、CRMPage、NavBar）。

- 2026-06-25｜師傅端「定位回推抵達時間（ETA）」差異化。（Claude）
  - 🔴 **下一步（待做，需人工）：在 Supabase SQL Editor 執行 [`supabase_eta.sql`](sql/supabase_eta.sql)**（`customers` 加 `lat`/`lng`；`repairs` 加 `eta_at`/`eta_text`/`accepted_at`/`arrived_at`/`est_distance_km`/`eta_source`）。未執行前接案寫 ETA 會在 sync 失敗。
  - 差異化核心：維修接案不再只推罐頭訊息，改為**接案當下讀師傅 GPS（限當次、只存距離不存座標）→ OSM Nominatim 把客戶地址轉座標（快取 `customers.lat/lng`）→ Haversine 算距離 → 用該師傅過去「接案→抵達」實際時程校準（min/km 中位數，冷啟動 fallback 市區車速）→ 回推時間範圍 ETA**，推播給客戶「預計今天 14:05–14:25 抵達」。
  - 退路：抓不到 GPS 或地址無法轉座標 → 跳手選時間範圍 sheet（+30/+60 分等），`eta_source='manual'`、不採計校準，仍能推播。
  - ⚠️ **OSM 台灣地址限制與修正**：實測發現 Nominatim 對「含門牌號」的台灣地址幾乎都回 NO_RESULT。新增 `addrVariants` 逐級降級鏈（① 去號保留路/段 ② 砍到 路/街/段 ③ 縣市+區 中心），實測 4 個真實地址 3 個拿到街道級、1 個退區級座標——配個人化校準精度已足夠。
  - 師傅端（`waferlock_tech.html`）：新增 `haversineKm`/`geocodeCustomer`/`getWorkerMinPerKm`/`estimateEta`/`manualEta` 純函式；改寫 `accept` 為「讀定位→估算→確認 sheet（可微調）→寫入」；`submitReport` 結案時補寫 `arrived_at`（校準閉環）；進行中卡片顯示「🚗 預計 … 抵達」。
  - CRM 端（`waferlock_crm.html`）：`dbToRepair`/`repairToDb` 補 6 個 ETA 欄位映射；維修卡片 `dispatched` 時於「⏰ 預約」旁顯示「🚗 預計抵達」。
  - 校準閉環權衡：以「結案時戳」當實際抵達時間（略高估耗時），校準層會吸收系統性偏差；要更準可後續加「📍 標記到場」按鈕（列為可選）。
  - ✅ 本機預覽驗證：師傅端所有函式正確定義、登入頁渲染、Haversine（台北→高雄 ≈297km）、`estimateEta`/`manualEta` 範圍正確、ETA sheet 渲染＋微調（+20 分位移正確）＋gps/manual 快捷鈕差異正確；CRM Babel 編譯通過、登入頁正常。
  - ✅ **正式環境（Supabase）端到端驗證**：6 個 `repairs` 新欄位＋`customers.lat/lng` 確認可讀寫；真實 OSM geocode（台中北屯崇德二路一段→24.176,120.695）→ Haversine（火車站→該址 4.48km）→ 校準（W1 無歷史回 fallback 2.4 min/km）→ ETA 範圍（10:01–10:21）→ 寫入測試工單 → 讀回 `eta_text`/`est_distance_km`/`eta_source` 皆正確 → 客戶 lat/lng 快取寫入可落地。**測試工單已刪除、客戶快取已還原，正式環境無殘留。**
  - 影響檔案：新增 `supabase_eta.sql`；`waferlock_tech.html`、`waferlock_crm.html`。
  - ⚠️ **2026-06-25 缺口修補（Claude）**：稽核時發現此功能雖記錄「已完成」，但 `waferlock_tech.html`（師傅端 136 行）與 `supabase_eta.sql` **先前漏未進版控**——只有 CRM 端顯示邏輯被夾帶部署，師傅端寫入邏輯沒上線（線上接案不寫 ETA → CRM 永遠顯示不出）。已補提交並 push（`bca74c0`），重新本機驗證純函式通過。教訓：功能完成後須確認**所有影響檔案都已 commit**，不能只看單一檔。

- 2026-06-23｜MD 5 集團→建案階層（資料）：新增 [`supabase_md5_seed.sql`](sql/supabase_md5_seed.sql)，含 `groups`/`projects` 建表＋10 間真實台灣建商×各 10 個真實建案（共 100，公開資料查證）。
  - ✅ **SQL 已於正式 Supabase 執行並驗證**（`groups` 10 筆、`projects` 100 筆，每集團各 10 筆）；`customers` 已加 `group_id`/`project_id` 欄位。
- 2026-06-23｜MD 5 集團→建案階層（前端 UI）：在 `waferlock_crm.html` 接上資料層已完成的階層。（Claude）
  - 資料層：`loadFromSupabase` 載入 `groups`/`projects`；新增 `dbToGroup`/`dbToProject`；`custToDb`/`dbToCust` 補 `group_id`/`project_id`（沿用既有同步模式，groups/projects 為唯讀參考資料不在前端編輯）。
  - 新頁「🏢 集團/建案」（`GroupProjectPage`）：三層瀏覽 集團 → 建案 → 建案詳情；建案詳情列出已歸屬客戶與其案件（報修/客訴），可一鍵跳完整 CRM。
  - 客戶詳情頁新增「🏢 歸屬建案」按鈕與 modal（集團→建案連動下拉），寫入客戶 `groupId`/`projectId` 並補「建案客戶」標籤；詳情頁顯示目前歸屬。
  - 歸屬層級放在「客戶」，案件透過所屬客戶歸屬（避免動 `repairs`/`complaints` 無此欄位）。
  - ✅ 已在本機預覽驗證：階層瀏覽正確、歸屬寫入正式 Supabase 成功、建案詳情正確顯示該客戶與案件；驗證用的測試歸屬已還原。
  - 影響檔案：`waferlock_crm.html`（資料層、`CRMPage`、新增 `GroupProjectPage`、`App` 導覽與路由）。
- 2026-06-24｜統一「維修工單＋安裝工單」為一個版本（比照 KingNet 約工單）。（Claude）
  - 🔴 **下一步（待做，需人工）：在 Supabase SQL Editor 執行 [`supabase_workorder_v3.sql`](sql/supabase_workorder_v3.sql)** 為 `installs`/`repairs` 加欄；未執行前統一表單建單會在 sync 階段失敗（前端會建立但無法寫回雲端）。
  - 新增共用元件 `WorkOrderForm`：單一「＋ 新增工單」表單，第一欄選 安裝／維修，依類別動態顯示專屬欄位；送出時仍寫回既有 `installs`/`repairs` 雙表（不做資料遷移）。
  - 兩入口共用此表單：`InstallPage` 的新增鈕（改名「＋ 新增工單（安裝／維修）」）、`CRMPage` 客戶詳情「🔧 新增派工」（鎖定為維修＋鎖定客戶）。
  - 新增欄位（installs/repairs 同步加）：承租身份(MD3 屋主/家人/租客＋租客需房東確認勾選)、來源通路、社區特殊需求、郵遞區號、工單類別細項、台數、預估工時、時間範圍(早/午/晚)、轟炸戶旗標、通用備註。
  - 可變工時：工時可填小數(如 1.5)，排程實際佔格＝`Math.ceil(工時)`，沿用既有 `bookSlots` 與整點 13 格衝突檢查（不改 bookings 結構）；`InstallPage.doSchedule`、`RepairPage.doDispatch` 已改用之。
  - `ChargeTable` 收費品項改「預設下拉(`CHARGE_PRESETS`)＋仍可手打」；維修派工註記區加「轟炸戶」。
  - 移除被取代的死碼：`InstallPage.createOrder`/`f` 狀態、`CRMPage.createCase`/`caseForm`。
  - ✅ 本機預覽驗證：頁面無編譯錯誤；統一表單從兩入口皆可開；安裝/維修類別切換正確顯示對應欄位；租客顯示房東確認提示；CRM 入口鎖定維修＋帶出客戶。
  - ✅ **已執行 `supabase_workorder_v3.sql` 並端到端驗證**：安裝/維修各建一張，新欄位（zip/work_hours/qty/tenant_type/landlord_confirmed…）皆正確寫入正式 Supabase；客編識別正常；測試資料已清除。
  - 影響檔案：`waferlock_crm.html`、新增 `supabase_workorder_v3.sql`。
- 2026-06-24｜地址選取畫面（中華郵政風格）＋ 工單收費品項 ＋ 第一頁排程。（Claude）
  - 🔴 **下一步（待做，需人工）：在 Supabase SQL Editor 執行 [`supabase_workorder_v4.sql`](sql/supabase_workorder_v4.sql)**（`customers` 加 `zip`、`installs` 加 `charges`/`charge_total`；repairs 既有）。未執行前，帶收費品項的安裝單或客戶 zip 會在 sync 失敗。
  - 新增 `AddressPicker` 元件＋內嵌全台 `TW_ZIP3`（縣市→鄉鎮→3碼，中華郵政開放資料）：選縣市→鄉鎮自動帶郵遞區號與基底地址，再自行輸入 路/段/巷、棟(選填)、號、樓(選填)、之(選填)，確認後整包帶回。**地址輸入處全面改用**：統一工單 `WorkOrderForm`、CRM「編輯客戶資料」。移除原本分開的郵遞區號/地址/樓棟/戶號欄位。
  - `WorkOrderForm` 下方新增「🛒 購買/收費品項」（沿用升級後的 `ChargeTable`：預設品項下拉＋手打、數量×單價＋贈品＋合計），安裝+維修建單即存（`charges`/`chargeTotal`）。
  - 第一頁「預約時間」欄＋「📅 開啟排程畫面」可選：直接選區域/師傅/時段，建單時一併佔格（`ceil(工時)` 格，衝突則中止）；未開則僅建單、事後再排。
  - 資料層：`installs` 加 `charges`/`charge_total` 映射；`customers` 加 `zip` 映射。
  - ✅ **已執行 v4 SQL 並端到端驗證**：建安裝單實測，`address`/`zip`(806/407)、`charges`(更換鋰電池×2=1000)/`charge_total`、客戶 `zip` 皆正確寫入正式 Supabase；測試資料已清除。
  - 修正：工單建立「新客戶」時一併寫入客戶 `zip`（先前只寫到工單、客戶 zip 為 null）。
  - 影響檔案：`waferlock_crm.html`、新增 `supabase_workorder_v4.sql`。
- 2026-06-24｜品項二層選單改造：安裝（全型號分系列）＋ 維修 ＋ 移除「安裝產品」欄位。（Claude）
  - 新增常數 `INSTALL_MODELS`：D系列(D300/D303/D310/D312)、C系列(C210/C760)、L3系列(16款含L375/L775雙版)、L6系列(L600)、L7系列(L701)、L9系列(L900/L901)、R400，共 28 款，來源：WAFERLOCK全型號知識庫.xlsx。
  - `ChargeTable` 品項欄改為二層：第一層選「安裝/維修」，第二層依類別展開：安裝→型號以系列 `<optgroup>` 分類；維修→保留原 8 個品項（含手打覆寫）。
  - `WorkOrderForm`：移除「安裝產品」`<select>` 欄（原只列 5 款舊型號）；改以 `useEffect` 監聽品項列，選「安裝」類別並挑型號後自動回填 `productId`（供保固計算 fallback `PRODUCTS[productId]?.warranty||36`）。
  - 目的：對應 ERP 商品編號，未來可直接比對。
  - ✅ 本機預覽驗證：類別欄 [— 類別 —, 安裝, 維修]；安裝→28 款 7 系列 optgroup；維修→8 品項；「安裝產品」label 已消失；console 無錯誤。
  - 影響檔案：`waferlock_crm.html`（`INSTALL_MODELS` 常數、`ChargeTable`、`WorkOrderForm`）。
- 2026-06-24｜客編流程修正：先建客戶主檔 ＋ 安裝工單建案聯動。（Claude）
  - **盲點修正**：客編不應是工單的副產品，客戶應先於工單存在；B2B 安裝工單建單時應能指定建案歸屬。
  - `WorkOrderForm` 購買通路選「建案配置」時，聯動顯示「所屬集團」和「所屬建案」下拉（10 間真實建商 × 各 10 建案），送出時自動寫入新/既有客戶的 `groupId`/`projectId`。
  - 新增 `CreateCustomerModal` 元件：客服可不依附工單，直接從首頁「＋ 新增客戶主檔」建立客戶（姓名＋地址必填，購買通路可選）；建案配置通路亦支援集團/建案聯動；查重機制（`custKey`）：已存在則跳至 CRM 詳情不重建。
  - 新客戶 tag 為 `['已登錄']`（區別於工單自動建立的 `待安裝`/`待整理`）。
  - `custToDb`/`syncToSupabase` 已含 `group_id`/`project_id` 映射，無需另改資料層。
  - ✅ 本機驗證：首頁「＋ 新增客戶主檔」modal 正確開啟；選建案配置→集團/建案下拉出現（遠雄建設、興富發建設…）；安裝工單同樣聯動；console 無錯誤。
  - 影響檔案：`waferlock_crm.html`（新增 `CreateCustomerModal`、`HomePage`、`WorkOrderForm`）。
- 2026-06-24｜CRM 主檔統一觸發工單 ＋ 移除派工頁面。（Claude）
  - **A. WorkOrderForm 解除類別卡控**：移除 `initialCategory="repair"` 及 `disabled` 邏輯，CRMPage 開啟的工單 modal 可自由選安裝或維修。
  - **B. CRMPage 加入工單推進操作**：新增維修排程/結案按鈕；新增安裝工單區塊（排程、已到場、施工完成、完工驗收+保固登錄、無法安裝）；內嵌 SlotPicker 半頁排程介面及 WorkOrderModal 驗收電子工單。
  - **C. 編輯客戶資料 modal 補建案聯動**：購買通路改為「建案配置」時顯示集團/建案下拉，儲存時同步寫入 `groupId`/`projectId`。
  - **D. 移除派工處理頁面**：刪除 `RepairPage`、`InstallPage`、`ComplaintPage`、`DispatchCenter` 元件；移除 NavBar「🛠️ 派工處理」按鈕及 `page==='dispatch'` 路由；移除已無用的 `pendingCnt`/`installPending`/`complaintPending`/`urgentCnt` 計數器。
  - ✅ 本機驗證：NavBar 剩 3 頁簽（首頁/工務時刻表/集團建案）；CRMPage 顯示「🛠️ 安裝工單（1 筆）」及「🔬 完工驗收」按鈕；console 無錯誤。
  - 影響檔案：`waferlock_crm.html`（移除約 530 行舊頁面；修改 `WorkOrderForm`、`CRMPage`、`App`）。

---

## 📦 版本歷程（新→舊）

### v0.9 — 統一工單・地址選取・照片 Storage・案件 Deep Link・CRM 流程重構（2026-06-26）

> 06-24～06-26 大批迭代封版。需人工執行的 SQL（皆已執行）：`supabase_md5_seed.sql`、`supabase_workorder_v3.sql`、`supabase_workorder_v4.sql`、`supabase_eta.sql`、`supabase_storage.sql`。

- **集團→建案階層（MD5）**：`groups`/`projects`（10 建商×各 10 建案）＋`GroupProjectPage`＋客戶歸屬建案。
- **統一工單 `WorkOrderForm`**：安裝/維修單一表單，新增承租身份/來源通路/社區需求/zip/工時/台數/時間範圍等欄；可變工時 `ceil` 佔格；`ChargeTable` 預設品項下拉。
- **地址選取器＋收費品項**：`AddressPicker`＋全台 `TW_ZIP3`（縣市→鄉鎮→3碼）全站套用；工單收費品項建單即存（`charges`/`charge_total`）；客戶/工單 `zip`。
- **品項二層選單**：`INSTALL_MODELS` 28 款分系列；移除「安裝產品」欄（對應 ERP 商編）。
- **客編流程**：先建客戶主檔＋建案聯動（`CreateCustomerModal`）；CRM 主檔統一觸發工單、移除舊派工頁面（刪 ~530 行）。
- **照片改存 Supabase Storage**：根治開頁慢——`loadFromSupabase` 21MB→127KB、6939ms→328ms；`uploadPhotos`；既有照片遷移、表只存 URL。
- **師傅端 ETA 定位回推抵達時間**：接案讀 GPS→OSM geocode→Haversine→個人化校準→回推時間範圍並推播客戶。
- **LINE 推播修正**：雙軌（寫對話＋發真實 LINE）；`pushCustomer` 從 `conversations` 查真實 `U...` uid；未綁定顯性提示；實機驗證 LINE App 收訊正常。
- **報修工單編號統一**：LINE 端 `createCase` 改走 `next_case_no`；清測試資料；全標準格式 `R+日期+4碼`。
- **階層式 Deep Link `#crm/{wfId}/{caseId}`**：客訴C/維修R/安裝IW 每張 case 專屬網址，貼網址即定位＋聚焦；相容舊 `#repair/#install` 並自動升級。
- **案件獨立視圖＋客戶主檔精簡＋歷史紀錄 Modal**：點案件開新分頁單張視圖（不混其他單）；客戶主檔改「📜 歷史紀錄」Modal 看全部過往聯繫/維修/派工。
- **Realtime 增量更新**：`postgres_changes` 改只更新受影響單筆，不再全量重拉（egress 優化）。
- **UI 修正**：工單/排程送出按鈕被 NavBar 遮蔽（底部固定按鈕）。
- **客訴流程重設計**：狀態重設計（pending 待處理／following 追蹤中／dispatched 已派工）；首頁只顯示 pending；「🔍 標記追蹤」設追蹤日期＋備註存 timeline JSONB；「🔧 建立派工」建單後自動連結並改狀態；側邊欄「追蹤中客訴 N 件」快捷入口。

### v0.8 — 客編識別改為「地址+姓名」（2026-06-23）
- 領域知識更正：**電話非唯一**（同號可能多住所：房東/房仲/包租代管），客編唯一鍵改為**正規化地址 + 姓名**
- 新增 `normAddr`/`custKey` 工具（全形轉半形、去空白、臺→台）
- `createOrder`（安裝建單）：地址改必填，以「地址+姓名」查既有客戶（不再用電話）
- 「整理客編」改為依「地址+姓名」歸戶，**冪等**：判定「案件地址+姓名與其客編對不上」才需整理；修正先前用電話誤併者；清除無案件引用的待整理孤兒客編
- `CaseDetailPanel` 聚合改以客編(wfId)為主，電話僅在無客編時輔助（避免同號不同住所混在一起）
- 影響檔案：`waferlock_crm.html`（全域工具、`InstallPage.createOrder`、`HomePage.runBackfill`、`CaseDetailPanel`）

### v0.7 — 客編中心化收尾：未歸戶案件整理（2026-06-23）
- 首頁新增管理員專用按鈕「🔗 整理未歸戶案件（N）」：把沒有客編的案件依電話歸到既有客戶，無對應則新建客編（tag `待整理`）
- 同電話多筆案件會集約到同一客編；已存在客戶則直接沿用、不重建
- 影響檔案：`waferlock_crm.html`（`HomePage`）
- 補充：報修/客訴從 CRM 建立時本就帶客編（`createCase`/`createComplaint`），新案件無此問題

### v0.6 — 工務時刻表點擊查看 + 異動紀錄顯示（2026-06-22）
- 工務時刻表格子顯示案件編號（R…/IW…），點格子 → 右側滑出案件詳情
- 案件詳情新增「📝 本案異動紀錄」區，列出 時間・動作・操作者（repair/install 取 history，complaint 取 timeline）
- 影響檔案：`waferlock_crm.html`（`SchedulePage`、`CaseDetailPanel`）

### v0.5 — 使用者登入系統（2026-06-22）
- `aa8c595` 新增使用者登入系統：帳密登入 + 視角權限 + 姓名浮水印 + 案件異動紀錄（寫入）
- 對應 SQL：`supabase_users_history.sql`（已於正式 Supabase 執行）

### v0.4 — CRM 首頁整合與客編中心化（2026-06-22）
- `1cdad27` 合併 安裝/報修/客訴 為單一「派工處理」頁籤
- `a14487a` 客編改為案件發生時即發行（安裝全程歸屬同一客編）
- `28e77fb` 移除重複的 CRM 客戶與場域搜尋分頁，整合至首頁
- `a093261` 首頁搜尋改為候選客戶清單→跳轉完整 CRM 詳情頁
- `24ac624` 首頁搜尋改為客戶導向（輸入電話→直接進客戶完整 CRM）
- `0b6b9f1` CRM 首頁整合案件大池（客訴單列表）+ 客戶歷程詳情面板

### v0.3 — 派工 / 安裝工單 / 工務時刻表（2026-06-21）
- `d3529fa` 師傅端加入安裝工單支援（到場/施工完成/照片簽名回報）
- `3067193` 工務時刻表改為 X軸=師傅、Y軸=時間
- `7c484f6` 全頁派工/安裝工單 + 分區排程(13格滿工) + 工務時刻表 + 不保固註記 + 收費品項
- 對應 SQL：`supabase_dispatch.sql`

### v0.2 — LINE 整合與師傅端（2026-06-18）
- `90fc0e4` index.html 加師傅端入口卡 + rich menu 圖
- `bf64d32` LINE 訊息加 sender 標示來源（AI/真人/客服）
- `bb03280` 新增獨立師傅端 + 案件編號規則（R/C+YYYYMMDD+流水）
- `ddd4962`/`78bfe9f`/`dd4674f` LINE 圖文選單（可切換分頁、規格修正）
- `3d7c0e2` 修正推播了但收件匣沒紀錄（以 lineUid 對應對話寫入）
- `5f224e4`/`41dfea7` CRM / 客服回覆透過 line-push 推回真實 LINE
- `d570dfa` 新增 LINE Messaging Edge Functions（webhook / push）
- `83059af`/`88382cd` LIFF 三合一（綁定/保固查詢/報修）+ Supabase schema v2 草案

### v0.1 — 基礎建設（2026-06-17）
- `d352378` 新增 index.html 入口頁
- `81bc04d` 遷移至 Supabase 資料庫

---

## 🛠️ 協作守則（給所有協作者，含 AI）

1. **改動前**：先讀本文件「目前狀態」與「未發布」，確認沒有人正在做同一塊。
2. **改動中**：遵守 `CLAUDE.md` 的編程契約（外科手術式修改、約定優先、顯性失敗）。
3. **改動後**：在「未發布」補一筆，格式：`- 日期｜做了什麼｜影響的檔案/功能｜（你的名字）`。
4. **commit**：訊息用繁體中文描述「做了什麼」。
5. **發布**：把「未發布」的內容整理成新版本區塊，push 到 `main`（即自動部署正式環境）。
